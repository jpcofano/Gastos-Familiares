# F6.1 — Infraestructura de comprobantes: Storage + subida + dedup por hash

> Primera pieza de backend del proyecto. Esta fase NO extrae datos (eso es F6.2) ni matchea/confirma
> (F6.3). Solo: configurar Storage + emulador, subir un archivo, deduplicar por SHA-256, y registrar el
> comprobante en Firestore. Testeable solo: subir → se guarda; re-subir el mismo → lo frena.

## Estado inicial (verificado)
- No existe `storage.rules`, ni emulador de Storage en `firebase.json`, ni `getStorage()` en `src/firebase.ts`.
- `storageBucket` ya está en el config del cliente.
- `sha256Hex` existe en `scripts/seed/utils/hash.ts` (Node/crypto) — pero el hash del archivo se calcula
  en el **cliente** (browser), así que hace falta una util con WebCrypto, no la de Node.

## Regla de trabajo
Investigá y reportá; esperá OK. Confirmá:
1. Versión del SDK de Firebase en el cliente (para `firebase/storage`).
2. Si hay una colección/tipo de `comprobantes` ya previsto en `src/types/index.ts` (el modelo del seed mencionaba `pdfHash`).
3. Dónde conviene el punto de entrada de subida (¿botón en `/resumen`, en una vista nueva, o global?).

## Cambios

### 1. `firebase.json` — emulador de Storage
- Agregar bloque `"storage": { "rules": "storage.rules" }` y `"emulators": { "storage": { "port": 9199 } }`.

### 2. `storage.rules` (nuevo)
- Path: `comprobantes/{hash}` (o `comprobantes/{familiaId}/{hash}`). Sugerido sin familiaId (un solo hogar).
- `allow read: if esMiembro()` — replicar el helper de auth por `request.auth.token.email` (mismo criterio que Firestore tras el P0; en Storage Rules el token también trae email).
- `allow write: if esMiembro() && request.resource.size < 10 * 1024 * 1024 && request.resource.contentType.matches('application/pdf|image/.*');`
- `allow delete: if esAdmin();`
- Nota: Storage Rules NO pueden hacer `get()` a Firestore para chequear rol. Resolver `esMiembro`/`esAdmin`
  por `request.auth != null` + (si se quiere rol) un **custom claim** o lista de emails embebida. Para v1,
  `request.auth != null` alcanza (cualquier autenticado del hogar sube); el control fino queda en Firestore.

### 3. `src/firebase.ts` — inicializar Storage
- `export const storage = getStorage(app);`
- Conectar al emulador: `connectStorageEmulator(storage, 'localhost', 9199)` bajo el mismo flag que ya usás para Firestore/Auth.

### 4. Util de hash en cliente — `src/datos/hashArchivo.ts` (nuevo)
- `async function sha256Archivo(file: File): Promise<string>` usando `crypto.subtle.digest('SHA-256', arrayBuffer)` → hex.
- (No reusar la util de Node de `scripts/seed`: esa corre en el seed, no en el browser.)

### 5. Modelo + datos — `comprobantes/{hash}`
- Tipo `Comprobante` en `src/types/index.ts`: `{ hash, nombreArchivo, contentType, tamano, rutaStorage, subidoPor, subidoEn, estado: 'subido' }`.
  (`estado` evoluciona en F6.2/6.3: `subido → extraido → vinculado`.)
- `src/datos/comprobantes.ts` (nuevo):
  - `subirComprobante(file)`:
    1. `hash = sha256Archivo(file)`.
    2. Chequear `getDoc(comprobantes/{hash})` → si existe, devolver `{ duplicado: true, comprobante }` (NO re-sube).
    3. `uploadBytes(ref(storage, 'comprobantes/'+hash), file)`.
    4. `setDoc(comprobantes/{hash}, {...})` con `subidoEn: serverTimestamp()`, `subidoPor: miMemberId()`, `estado:'subido'`.
    5. Devolver `{ duplicado: false, comprobante }`.
  - El doc-id ES el hash → el dedup de archivo es automático e idempotente.

### 6. Rules de Firestore — colección `comprobantes`
- `match /comprobantes/{hash} { allow read: if esMiembro(); allow create: if esMiembro(); allow update, delete: if esAdmin(); }`

### 7. UI mínima de prueba
- Un botón "Subir comprobante" (donde definas en el reporte) que toma un `File`, llama `subirComprobante`,
  y muestra: "subido" o "este comprobante ya estaba cargado" (dedup). Sin extracción todavía.

### 8. Ciclo limpio
`rmdir /s /q emulator-data` → emulators (ahora con Storage) → `npm run seed` → `npm run validate` → verde.

---

## Definición de hecho
- Emulador de Storage levanta en :9199 y aparece en la UI del emulador (:4000).
- Subir un PDF/imagen crea el archivo en `comprobantes/{hash}` + el doc en Firestore.
- Re-subir el MISMO archivo → detecta duplicado por hash, NO crea un segundo archivo ni doc.
- Un PDF > 10MB o un tipo no permitido → la subida es rechazada por las Storage Rules.
- `validate` verde.

## Sigue (no en este prompt)
- **F6.2**: Function (primer backend de cómputo) + prompt de extracción endurecido (tipoDocumento primero,
  CUIT-vs-número, pseudo-número para recurrentes) → llena el comprobante con datos extraídos (`estado:'extraido'`).
- **F6.3**: match contra movimientos/esperados → 3 ramas del pipeline (confirmar / crear-con-confirmación / alta precargada).
