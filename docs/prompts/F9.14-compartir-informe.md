# F9.14 — Compartir informe del Dashboard (a definir mecanismo)

> Reglas: doc-first · una PR por fase · sin tocar Firestore/Functions en la PR visual.
> Estado: **placeholder** ya presente en el mock del kit (botón "Compartir informe" al
> pie del Dashboard Mensual). El mecanismo se define más adelante.

## Qué
Acción para **compartir el informe mensual** desde el Dashboard. Hoy en el kit es un
botón outlined full-width al pie de la vista Mensual (icono `lucide` `Share2` +
"Compartir informe"); abre un placeholder.

## Opciones de mecanismo (a decidir)
La legacy ya enviaba el dashboard mensual por **email** (hoja Config: "Emails dashboard
mensual (TO/CC)", "Día/hora de envío", "habilitado SI/NO"). Posibles caminos:
1. **PDF**: render del informe a PDF y compartir/descargar (Web Share API en mobile).
2. **Email**: reusar el envío mensual existente, disparado manualmente.
3. **Link**: link de solo lectura con TTL (la planilla ya tenía `_ShareTokensTmp`,
   "Share Token TTL horas", "Share Max Size MB" → ya existe infraestructura de share).

## Pendiente
- Definir cuál(es) de los 3 y el alcance (mes actual vs rango).
- Recién entonces: prompt de implementación (puede tocar Functions → PR aparte, NO visual).
