# F6.2.1 — Extracción de comprobantes (prompt endurecido)

> **Modo de trabajo**: investigá y reportá ANTES de escribir. Esperá aprobación explícita.
> Emulador-only. Requiere F6.2.0 cerrado (trigger + download + writeback ya andando con stub).

## Objetivo

Reemplazar el stub de F6.2.0 por la extracción real: llamar a Anthropic con el archivo bajado, parsear el JSON, poblar `datosExtraidos` y marcar `'extraido'`. Fallo de API o JSON inválido → `'error'`.

## Decisiones ya tomadas (no reabrir)

- **Modelo**: `claude-sonnet-4-6` (clasifica bien + JSON estructurado confiable con PDF/imagen). Haiku queda de reserva si pesa el costo.
- **Secret**: `ANTHROPIC_API_KEY` por Secret Manager (`defineSecret`), bindeado a la function. En emulador vía `functions/.secret.local` **gitignored**.
- **La extracción nunca auto-confirma**: propone `datosExtraidos`, el humano confirma en F6.3. Esta fase solo llena el comprobante.
- **Sub-objeto `datosExtraidos`**: el shape ya quedó en el tipo en F6.2.0. Acá se puebla.

## PASO 1 — Investigá y reportá (sin escribir)

1. Confirmá cómo quedó resuelto el `contentType` en F6.2.0 (campo en el doc vs `getMetadata()`). El bloque de contenido es **document** (PDF) o **image** (con su `media_type`) según eso.
2. Confirmá que `functions/.secret.local` esté en `.gitignore` (o agregalo). Reportá antes de crear el archivo de secret.

**Pará acá y esperá OK.**

## PASO 2 — Implementación (recién con aprobación)

### Dependencias y secret
- Instalar `@anthropic-ai/sdk` en `functions/`.
- `const anthropicKey = defineSecret('ANTHROPIC_API_KEY');` y bindearlo: `onDocumentCreated({ document: 'comprobantes/{hash}', region: 'southamerica-east1', secrets: [anthropicKey], timeoutSeconds: 120, memory: '512MiB' }, ...)`.
- `functions/.secret.local` con `ANTHROPIC_API_KEY=...` para el emulador (gitignored).

### Llamada
- Bajar bytes (ya implementado en F6.2.0) → base64.
- Armar bloque de contenido:
  - PDF → `{ type: 'document', source: { type: 'base64', media_type: 'application/pdf', data } }`
  - imagen → `{ type: 'image', source: { type: 'base64', media_type: <contentType>, data } }`
- User message: el bloque + texto `"Extraé este comprobante."`.
- `system`: el prompt endurecido de abajo, textual.
- `max_tokens: 1024`.

### Prompt de extracción (system, textual)

```
Sos un extractor de datos de comprobantes argentinos (facturas, tickets, comprobantes de pago/transferencia, resúmenes de tarjeta, recibos de servicios).

Devolvés EXCLUSIVAMENTE un objeto JSON válido. Sin markdown, sin ```json, sin texto antes ni después.

PASO 1 — Clasificá tipoDocumento ANTES que nada. Esto decide dónde están los demás campos.
Valores: "factura_a" | "factura_b" | "factura_c" | "ticket" | "comprobante_pago" | "transferencia" | "resumen_tarjeta" | "recibo_servicio" | "otro".

PASO 2 — Extraé los campos según el tipo.

REGLAS DURAS:
- cuit: 11 dígitos, formato XX-XXXXXXXX-X. Es el identificador fiscal del emisor. NUNCA lo pongas en numeroOperacion. Si ves "CUIT", "C.U.I.T." o un número de 11 dígitos con ese patrón, va en cuit.
- numeroOperacion: el número de operación / transacción / comprobante / factura. NO es el CUIT. NO es el monto. NO es la fecha.
- Si el comprobante NO tiene número de operación (típico en recibos de servicios recurrentes), generá un pseudo-número con formato YYYY-MM-<slug>, donde YYYY-MM sale del período facturado (o de la fecha de emisión si no hay período) y <slug> es el comercio en minúsculas, sin espacios ni acentos. Ej: "2026-06-edesur".
- moneda: "ARS" o "USD". Inferí por símbolo ($ = ARS salvo que diga USD / U$S / dólares), contexto y emisor. Ante la duda, "ARS".
- fecha: ISO "YYYY-MM-DD", fecha de emisión.
- montoTotal: número, solo el total final a pagar/pagado. Sin símbolo, punto decimal, sin separador de miles. Ej: 15340.50.
- comercioRazonSocial: razón social o nombre de fantasía del emisor.

Si un campo no se puede determinar con confianza, usá null (excepto numeroOperacion, que siempre lleva número real o pseudo-número).

Esquema de salida EXACTO:
{
  "tipoDocumento": "...",
  "fecha": "YYYY-MM-DD" | null,
  "montoTotal": number | null,
  "moneda": "ARS" | "USD",
  "comercioRazonSocial": "..." | null,
  "cuit": "XX-XXXXXXXX-X" | null,
  "numeroOperacion": "..."
}
```

### Parseo y escritura
```
- raw = concatenar los bloques de texto de la respuesta, trim
- cleaned = sacar fences ```json defensivamente, trim
- try: parsed = JSON.parse(cleaned)
  - validar mínimo viable: tipoDocumento (no vacío) + moneda ('ARS'|'USD') + numeroOperacion (no vacío)
    (montoTotal/fecha/comercio/cuit pueden ser null → igual pasa a 'extraido'; el humano lo agarra al confirmar)
  - si falta lo mínimo → throw
  - update: { estado: 'extraido', datosExtraidos: parsed, errorExtraccion: FieldValue.delete() }
- catch:
  - update: { estado: 'error', errorExtraccion: <mensaje, incluí el raw truncado para debug> }
```

## Cierre

1. Ciclo limpio: `rmdir emulator-data` → `emulators:start` → `npm run seed` → `npm run validate`. Todo verde.
2. Prueba real con `.secret.local` cargado: subí 2-3 comprobantes reales (idealmente una factura con nro de operación, un recibo de servicio recurrente SIN nro → verificá el pseudo-número `YYYY-MM-`, y uno con CUIT visible → verificá que el CUIT NO se filtró a `numeroOperacion`).
3. Forzá un caso de error (archivo basura) → verificá `'error'` + `errorExtraccion`.
4. Commit + push: `F6.2.1 — extracción de comprobantes (Sonnet 4.6 + prompt endurecido)`.

> Próximo: **F6.3** — match de `datosExtraidos` contra movimientos/esperados → las 3 ramas del pipeline (existe→adjuntar+confirmar; solo esperado→crear con confirmación; nada→alta precargada).
