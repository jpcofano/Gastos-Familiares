# F4.0 — Castellanización total del modelo (cierre de deuda P3)

**Fase:** F4 · **Sub-paso:** 4.0 (prerequisito de todas las vistas de F4)
**Scope:** seed + tipos + rules + índices + re-seed contra emulador. NADA contra producción (que sigue vacía — por eso este es el momento barato).

---

## Contexto

El CLAUDE.md declara "castellano camelCase para colecciones y campos", pero el modelo real quedó híbrido (herencia del sistema viejo, confirmado por la auditoría). Esta inconsistencia ya causó dos bugs reales (`actualizadoEn`, `hashPDF`). Decisión cerrada: **castellano total, ahora**, antes de que F4 incruste los nombres en el cliente.

Ventana de oportunidad: el cliente actual solo lee `/config/familia` (cuyos campos ya están en castellano). Ninguna vista lee todavía las colecciones afectadas. Producción está vacía. Después de F4 este cambio costaría 10x.

---

## Antes de escribir — investigar y reportar

1. `grep` exhaustivo de CADA nombre de la tabla de abajo en TODO el repo (`scripts/`, `src/`, `firestore.rules`, `firestore.indexes.json`, `docs/`). Reportar la lista completa de archivos a tocar.
2. Confirmar que ningún archivo del cliente (`src/`) referencia los nombres viejos salvo `src/types/index.ts`.
3. **Esperar mi aprobación de la lista de archivos antes de tocar nada.**

---

## Tabla de renombres — CAMPOS

| Actual (inglés) | Nuevo (castellano) | Dónde aparece |
|---|---|---|
| `createdAt` | `creadoEn` | movements |
| `updatedAt` | `actualizadoEn` | movements |
| `parentId` | `padreId` | movements |
| `cardStatementId` | `resumenTarjetaId` | movements |
| `expectedItemId` | `itemEsperadoId` | movements |
| `pdfHash` | `hashPdf` | movements, cardStatements |
| `pdfStorageRef` | `refStoragePdf` | movements, cardStatements |
| `parsedAt` | `parseadoEn` | cardStatements |
| `confirmedAt` | `confirmadoEn` | cardStatements |
| `confirmedBy` | `confirmadoPor` | cardStatements |
| `autoCalendar` | `autoCalendario` | expectedItems |

Ya correctos (no tocar): `creadoPor`, `actualizadoEn` (en config), `numeroComprobante`, `montoEsperado`, `diaVencimiento`, `excluirDash`, `incluirResumenMes`, y todos los demás en castellano.

Nota sobre `hashPdf`: sí, este campo se renombró en F3.1.3 (`hashPDF` → `pdfHash`, unificación) y ahora se traduce (`pdfHash` → `hashPdf`). Son dos correcciones distintas: primero unificar, ahora castellanizar. Esta es la forma final; no se toca más.

## Tabla de renombres — COLECCIONES

| Actual (inglés) | Nueva (castellano) |
|---|---|
| `movements` | `movimientos` |
| `cardStatements` | `resumenesTarjeta` |
| `expectedItems` | `itemsEsperados` |
| `dictionary` | `diccionario` |
| `normalizationRules` | `reglasNormalizacion` |
| `tcDaily` | `tcDiario` |
| `users` | `usuarios` |

Ya correctas (no tocar): `config`, `subcategorias`, `etiquetas`.

---

## Qué tocar (las cuatro patas, juntas)

1. **`src/types/index.ts`**: renombrar los campos de la tabla en `Movement`, `CardStatement`, `ExpectedItem`. Los nombres de interfaz quedan en inglés (`Movement`, `CardStatement` son nombres de TIPO, no de datos — convención de código TS, no del modelo de datos; si preferís castellanizarlos también, proponerlo en el reporte, pero no es obligatorio).
2. **`scripts/seed/`**: los 9 transformers + validators + cualquier util que referencie campos o colecciones de las tablas. Renombrar TODO.
3. **`firestore.rules` + `firestore.indexes.json`**: actualizar nombres de colecciones y de campos indexados. OJO: los índices compuestos referencian campos — revisar cada índice.
4. **`docs/CLAUDE.md`**: actualizar la sección "Modelo de datos" con los nombres nuevos, y la línea de "Audit" que decía `createdAt, updatedAt, creadoPor` → `creadoEn, actualizadoEn, creadoPor`. Mostrar diff antes de aplicar.

---

## Pasos operativos (los corro yo)

```powershell
rmdir /s /q emulator-data
npm run emulators        # terminal A
npm run seed             # terminal B
npm run validate         # objetivo: 8 OK / 0 FAIL
```

Después del verde: levantar el cliente (`npm run dev`) y verificar que login + resolución de miembro siguen funcionando (no deberían verse afectados — `config/familia` no cambió — pero verificar igual).

---

## Restricciones

- **NO** tocar producción.
- **NO** renombrar campos de `/config/familia` (ya están en castellano).
- **NO** cambiar lógica de ningún transformer — esto es SOLO renombres. Si al renombrar aparece un bug preexistente, reportarlo, no arreglarlo en este paso.
- **NO** avanzar a F4.1 hasta 8 OK / 0 FAIL + login verificado.

---

## Criterio de cierre

- `grep` de cualquier nombre viejo de las tablas (campos y colecciones) en el repo: CERO resultados fuera de `docs/` histórico y `docs/prompts/` (los prompts viejos son registro, no se editan).
- Re-seed: 8 OK / 0 FAIL.
- Login + "Hola {nombre}" funcionando.
- CLAUDE.md actualizado y sin contradicción interna de naming.
- Commit con mensaje "F4.0: castellanización total del modelo (campos + colecciones)".
