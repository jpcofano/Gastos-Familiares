# F9.29 — Conectar Cargar (Alta manual real + entrantes)

> Reglas: doc-first · una PR por pantalla. Basado en el mapa de F9.25. Cierra el hueco que
> dejó F9.22: el FAB abre Alta Manual, pero la UI de Cargar todavía llama a modales mock en
> vez del motor real (`AltaMovimiento.tsx`, que YA es real).

## Problema (del mapa F9.25)
- `AltaMovimiento.tsx` es **real** (`crearMovimiento`, `tcParaFecha`, `cargarSubcategorias`,
  clasificador) pero quedó **desconectado** de la UI nueva: `Comprobantes.tsx` sigue
  abriendo los modales mock.
- Dropzone / entrantes recientes = `EXAMPLE_ENTRANTES` (mock).

## Tarea
1. **Conectar el motor real**: el FAB "+" (alta rápida, F9.22) y "Cargar manualmente" abren
   el flujo de `AltaMovimiento.tsx` real (no el modal mock). Confirmar que el alta escribe
   con `crearMovimiento` y persona = **memberId** (F9.24).
2. **Entrantes recientes**: reemplazar `EXAMPLE_ENTRANTES` por la fuente real de pendientes/
   entrantes (la que corresponda en `datos/entrantes.ts`); estado ok/wait/warn real.
3. **Confirmar comprobante**: el flujo de confirmación enlaza al movimiento real (ya hay
   piezas reales en `datos/comprobantes.ts`).
4. **SeccionTarjetas** dentro de Cargar ya es real (no tocar).

## Aceptación
- Alta manual desde la UI nueva crea un `movimiento` real (con memberId), no mock.
- Entrantes recientes vienen de datos reales, sin `EXAMPLE_ENTRANTES`.
- El FAB de F9.22 termina en el motor real de alta.
- Sin regresión visual respecto de `CargaMobile.jsx` / `ManualGasto.jsx`.

## Notas
- Depende de F9.24 (persona→memberId) para escribir el alta canonizada.
- Ver `docs/plan-maestro-firestore-pwa.md` (Etapa A).
