# F5.3 — Realtime (onSnapshot + offline)

> **Modo de trabajo**: investigá y reportá ANTES de escribir. Esperá aprobación explícita.
> Emulador-only. Refactor 100% cliente (`src/`). Sin backend, sin cambios de tipos/seed.

## Motivo
El pipeline de comprobantes es asincrónico: la function escribe `extraido`/`error`/`propuestaMatch`
sin que la UI se entere → hoy refrescás a mano para ver cada cambio del trigger. Realtime ataca eso de
raíz. Beneficia además Resumen, Dashboard, Esperados y listas de movimientos.

## Principio clave (no sobre-ingeniería)
**Firestore ya hace latency compensation.** Una escritura local dispara el callback de `onSnapshot`
*al instante*, antes del ack del server, con `snapshot.metadata.hasPendingWrites === true`. Es decir:
con `onSnapshot` bien puesto, el "optimistic update" sale **gratis** — NO hay que mantener estado
optimista a mano salvo un caso puntual que lo justifique. Default: apoyarse en el SDK, no hand-rollear.

## PASO 1 — Investigá y reportá (sin escribir)
1. **Cómo leen hoy las vistas**: ¿hay capa de servicio (`listarComprobantes`, `listarMovimientos`, etc.
   con `getDocs` one-shot)? Listá qué vistas leen qué colecciones. El refactor cambia esos one-shot por
   suscripciones.
2. **¿Estado por-vista o store/context?** Si varias vistas consumen la misma colección (p.ej. movimientos
   en Resumen + Dashboard + Esperados), conviene **un listener compartido** (context) para no abrir N
   listeners de lo mismo. Si cada vista es independiente, **hook por colección** alcanza. Reportá el mapa
   vista↔colección para decidir.
3. **Init de Firestore**: dónde se hace `getFirestore`/`initializeFirestore` y la versión del SDK modular
   (define la API de offline: `persistentLocalCache` moderno vs `enableIndexedDbPersistence` deprecado).
4. **Escrituras existentes** (confirmar pago F5.5, alta de movimiento, CRUD esperados F5.4): para saber
   cuáles se benefician de la compensación de latencia y si alguna necesita feedback inmediato especial.

**Pará acá y reportá. El patrón (hooks vs context) lo cierro con el mapa vista↔colección.**

## PASO 2 — Implementación (recién con aprobación, ajustada a hallazgos)

### Suscripciones (reemplazar one-shot por onSnapshot)
- Patrón a definir en PASO 1:
  - **Hook por colección** (`useComprobantes`, `useMovimientos`, `useItemsEsperados`): `onSnapshot` en
    `useEffect`, devuelve datos + loading + error, **cleanup del unsubscribe** en el return del effect.
  - **Context compartido** para colecciones consumidas por varias vistas (un solo listener, N consumidores).
- Prioridad de cobertura: `comprobantes` (máximo valor), `movimientos`, `itemsEsperados`. Las vistas
  derivadas (Resumen, Dashboard) recomputan de los datos en vivo — no suscribir un "resumen", suscribir
  las fuentes y derivar.
- Colecciones casi-estáticas (`config`, `diccionario`, `etiquetas`, `subcategorias`): pueden quedar
  one-shot o suscribir con baja prioridad; decidir según el mapa. No malgastar listeners.

### Índices (mirando a producción — NO opcional)
Las queries de los listeners que combinan filtros necesitan índice compuesto declarado en
`firestore.indexes.json`, sino fallan en prod (el emulador no se queja):
- `movimientos`: `where(mes) + where(persona)` (Dashboard) → **índice compuesto**. Declararlo ahora.
- `comprobantes`: `orderBy(subidoEn desc)` single-field → OK, sin índice extra.
- Revisar cualquier otro `where+where` o `where+orderBy` que surja al refactorizar y declararlo.

### Offline
SDK confirmado ^10.12.0 (moderno) → usar `persistentLocalCache`:
`initializeFirestore(app, { localCache: persistentLocalCache({ tabManager: persistentMultipleTabManager() }) })`.
- **Orden de init crítico**: `initializeFirestore(...)` ANTES de `connectFirestoreEmulator(...)` y antes
  de cualquier uso de Firestore. Hoy `src/firebase.ts` usa `getFirestore(app)` sin cache → reemplazar.
- **Degradar elegante**: si la persistencia falla (incógnito, browser sin IndexedDB), capturar y seguir
  sin cache — no crashear la app.
- Multi-tab por `persistentMultipleTabManager`.

### Optimistic
- **Default: ninguno manual.** Apoyarse en latency compensation. Opcional: usar `hasPendingWrites` para
  un indicador sutil de "guardando…" donde sume, sin duplicar estado.

### Limpieza
- Todo `onSnapshot` con su `unsubscribe` llamado en cleanup. Sin listeners colgados al desmontar.
- Quitar los `getDocs` one-shot que queden muertos tras el refactor (no dejar las dos vías).

## Cierre
1. Build limpio + ciclo de emulador (volátil): levantar, seed, validate.
2. Prueba en vivo del valor central: subí un comprobante y verificá que `/comprobantes` pasa de
   `subido`→`extraido`→ propuesta **sin refrescar a mano** (el trigger escribe, la UI reacciona).
   Confirmá pago en una vista y verificá que otra vista abierta a la misma colección se actualiza sola.
3. Verificá offline básico: cortar red del emulador/recargar y ver que la cache responde.
4. `docs/CLAUDE.md`: cerrar F5.3 (realtime activo; latency compensation en vez de optimistic manual).
5. Push: `F5.3 — realtime (onSnapshot + offline)`.
