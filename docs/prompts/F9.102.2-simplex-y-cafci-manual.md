# F9.102.2 — Fix del optimizador de mínima varianza + ingesta manual CAFCI

## Contexto

Primera corrida de Optim con series reales (21/07/2026, post-F9.102.1). La
persistencia funciona, la matriz de correlaciones renderiza y el alineado por
semana ISO cumplió: 9 símbolos USD con serie (USDT, B, BTC, VZ, AAVE, GLOB,
CVX, ETH, ACN), los `.BA` excluidos por TC insuficiente como corresponde.

Pero aparecieron dos problemas.

### A. `calcMinVarianza` está roto — 2 tests en rojo

Salida real de `correrTests` en producción:

- `Min-var 2 activos no correlacionados (analítico)`: esperado `[0.8, 0.2]`,
  obtenido `[1.0000, 0.0000]`.
- `Min-var respeta wMax=0.4`: constraint violada, `w = [1.0000, 0.0000]`.

Causa raíz: `proyectarSimplex`
(`src/datos/patrimonioOptimizacion.ts:238`) no es la proyección euclídea
sobre `{w ≥ 0, Σw = 1, wᵢ ≤ wMax}`. Hace *clamp a [0,wMax] → renormalizar →
repetir*, que es una proyección **radial**: al clampear los negativos a cero
y luego reescalar proporcionalmente, empuja la solución a los vértices y
cualquier activo que toque el cero queda expulsado de forma irreversible.

Verificado a mano con el caso del test (Σ = diag(1,4), lr = 0.2):
partiendo de `w = [0.9, 0.1]` → gradiente `2Σw = [1.8, 0.8]` →
`w_new = [0.54, −0.06]` → clamp `[0.54, 0]` → renormaliza `[1.0, 0]`.
Punto fijo espurio, del que el descenso ya no sale.

Impacto en producción: la columna "Mín. var." asigna exactamente `wMax` a
cinco activos y 0% al resto (`5 × 20% = 100%`) — solución *bang-bang*, la
firma del bug. **Los números que se muestran hoy no son una cartera de
mínima varianza.** El bug existe desde F9.98 y estuvo tapado porque hasta
F9.102 todas las series se excluían y el optimizador nunca corría con datos.

`calcRiskParity` usa otro algoritmo (Newton cíclico, Maillard-Roncalli-
Teïletche) y sus tests pasan: esa columna es confiable y no se toca.

### B. Experimento de región: no concluyó

Lectura de `firebase functions:log --only sincronizarCafci`:

- Todos los 403 son del **20/07** (16:56 y 23:48 UTC), previos a la migración.
- 21/07 14:10 UTC `DeleteFunction` en `southamerica-east1`; 14:12–14:13
  `CreateFunction` en `us-central1`, ACTIVE.
- **Cero logs de invocación posteriores.** La prueba del usuario fue a las
  14:34 UTC — 20 minutos después del deploy — y no dejó rastro.

Causa: `src/firebase.ts:31` tiene
`getFunctions(app, 'southamerica-east1')` hardcodeado. La función ya no
existe en esa región, el llamado pega contra una URL inexistente, vuelve
404 con HTML en vez de JSON, y el SDK de callables mapea toda respuesta no
parseable a `internal`. De ahí el toast "falló (internal)": la request nunca
llegó a la función.

**Auditar antes de tocar**: verificar líneas y firmas citadas; si algo
difiere (F9.102/F9.102.1 movieron renglones), reportarlo antes de implementar.

---

## Alcance

### 1. Proyección euclídea al símplex con caja (bloqueante)

Reemplazar `proyectarSimplex` por la proyección euclídea real sobre
`{w ≥ 0, Σw = 1, wᵢ ≤ wMax}`, vía bisección sobre el umbral τ:

```
w(τ)ᵢ = clamp(vᵢ − τ, 0, wMax)
```

`Σ w(τ)ᵢ` es monótona no creciente en τ, así que la bisección converge
siempre. Buscar τ tal que la suma sea 1 (tolerancia 1e-12, ~100 iteraciones;
bracket inicial `[min(v) − 1, max(v)]`). Determinística, sin heurística, sin
loop de renormalización.

**Guarda de factibilidad**: el conjunto es vacío si `n × wMax < 1`. Con 9
activos elegibles y wMax 15% da 1.35 (holgado), pero si las series se
achican el caso es alcanzable. Si es infactible: usar
`wMaxEfectivo = max(wMax, 1/n)`, seguir adelante, y registrar el hecho en
`advertencias[]` del resultado + mostrarlo en la UI ("con N activos
elegibles, el tope de X% es infactible; se usó 1/N = Y%"). No fallar
silenciosamente ni tirar el cálculo.

Mantener la firma de `calcMinVarianza` y el descenso de gradiente proyectado;
lo único que cambia es la proyección. Verificar de paso que el paso `lr`
derivado de `eigMax` siga siendo razonable con la proyección correcta —
si con los tests nuevos converge lento, evaluar backtracking, pero **no**
tocarlo si los tests pasan.

### 2. La columna Mín. var. no se muestra si el motor no está sano

Mientras `calcMinVarianza` no pase sus tests, la columna no debe presentarse
como resultado utilizable. Implementar: al calcular, correr los tests del
motor de min-var (los mismos de `correrTests`, casos 1 y 3) y si alguno
falla, en "Carteras calculadas vs actual" mostrar la columna en gris con la
leyenda "no confiable — tests del motor en rojo" en vez de los pesos.

No es andamiaje temporal: es una guarda permanente que evita que un
resultado numéricamente inválido llegue a la pantalla como si fuera una
recomendación. Mismo criterio que "auditar antes de tocar", aplicado al
output.

### 3. Cerrar el experimento de región y dejarlo funcionando

#### 3a. Instancia de functions por región

En `src/firebase.ts`, además de la instancia global (que sigue en
`southamerica-east1`), exportar una segunda instancia
`functionsUsCentral = getFunctions(app, 'us-central1')`, y que
`handleSincronizarCafci` use esa para el callable.

#### 3b. Correr y reportar

Ejecutar Sincronizar y verificar en
`firebase functions:log --only sincronizarCafci` que **la invocación quedó
registrada en us-central1**. Criterio de lectura explícito: un `internal`
sin log de invocación significa que la request no llegó, NO que CloudFront
bloqueó. Solo con log presente la conclusión es válida.

- Si devuelve 200 con datos: consolidar `sincronizarCafci` en `us-central1`,
  documentar el porqué en un comentario, y dejar la instancia extra.
- Si vuelve 403 (con log): revertir la función a `southamerica-east1`
  (`functions:delete` en us-central1 + redeploy) y eliminar la instancia
  extra del cliente, para no dejar dos regiones sin motivo.

**En cualquiera de los dos casos, la app queda con Sincronizar funcionando
contra una región existente.** No dejar el estado actual, donde el cliente
apunta a una región donde la función no existe.

### 4. Ingesta manual de JSON CAFCI

Se construye **independientemente del resultado de 3**: es la red de
seguridad ante un bloqueo que no controlamos, y cierra la referencia
fantasma del mensaje de error.

#### 4a. Backend

Extraer el parseo de ficha de `sincronizarCafci` a un helper compartido
`parsearFichaCafci(json, fondo)` — **una sola implementación**, para que la
sync automática y la manual no diverjan. Mantiene todas las validaciones
F9.97.1: `success`, `data.info.semanal`, `carteras[]` con `nombreActivo`/
`share`, `fechaDatos` anidado, control de suma de shares, auto-mapping,
detección LIQUIDEZ/CEDEAR.

Nueva CF `importarCafciManual({ fondoId, claseId, json })`:

- Parsea el JSON crudo (string) con el helper compartido.
- Escribe en `cafciCarteras` con el mismo `docId` y la misma forma que la
  sync automática, más `origen: 'manual'` y `fechaIngesta`.
- Retorna `{ ok, especies, pendientesMapeo, fechaDatos }`.
- Errores claros y accionables: JSON inválido, `success: false`, estructura
  inesperada, fondo/clase que no coincide con lo pedido.

#### 4b. UI

En el card "Fondos CAFCI (benchmark)" de Config, por cada fondo:

- Un indicador de estado: fecha de datos si ya está cargado, o "sin datos".
  Así se ve de un vistazo cuáles faltan.
- Botón "Pegar JSON" → modal con (i) la URL exacta de la ficha, copiable y
  clickeable: `https://api.pub.cafci.org.ar/fondo/{fondoId}/clase/{claseId}/ficha`;
  (ii) textarea; (iii) botón Importar.
- Al confirmar: toast con especies importadas y fecha de datos, y refresco
  del indicador de estado.

#### 4c. Mensaje de error honesto

El mensaje de 403 pasa a apuntar a la feature real:
`HTTP 403 — CloudFront bloqueó la request. Usá "Pegar JSON" en Config.`

---

## Fuera de alcance

- Backfill de `tcDiario` (F9.103) — ahora sí desbloqueado: el diagnóstico de
  series ya persiste. Se define en sesión aparte con el dato de semanas de TC.
- Reintroducir Apps Script: descartado.
- VPC connector / Cloud NAT con IP estática.
- `calcRiskParity` y el parseo CAFCI F9.97.1: no se tocan.

## Verificación

1. `tsc --noEmit`: reportar el conteo **antes y después** (el baseline se
   movió de 48 a 41 entre sesiones sin quedar registrado; fijarlo ahora y
   anotarlo en el repo). `vite build` y build de `functions` OK.
2. `correrTests` desde la UI de Optim: **10/10 en verde**, incluidos los dos
   de min-var que hoy fallan.
3. Optim → Recalcular con datos reales: la columna Mín. var. deja de ser
   `wMax` en todos los activos no nulos; los pesos se reparten de forma
   dispersa y la volatilidad de mín. var. es **menor o igual** a la de risk
   parity (propiedad matemática: si no se cumple, el optimizador sigue mal).
4. Caso infactible: forzar wMax por debajo de `1/n` y verificar que aparece
   la advertencia y el cálculo no se rompe.
5. CAFCI: reportar log de invocación en us-central1 (presente o ausente) y el
   código de respuesta. Estado final de la app con Sincronizar apuntando a
   una región donde la función existe.
6. Ingesta manual: importar un fondo y verificar que el doc en
   `cafciCarteras` queda **idéntico en forma** al que produce la sync
   automática (comparar campo por campo con un doc previo si lo hay).

## Cierre de sesión

Commit + push obligatorio: mostrar salida de `git push` y
`git log --oneline origin/main -1` antes de reportar la tarea como
terminada. Mensaje sugerido:
`F9.102.2 -- Proyeccion euclidea al simplex + ingesta manual CAFCI`.
