# F4.2.1.1 — Fix de match de esperados: colisión de `id` (tarjetas) + `matchTexto` incluye/excluye

> Verificado contra el repo (commit `55759de`) y contra `data/2026-06-12_sheet_snapshot.xlsx`.
> Dos bugs independientes:
>
> **Bug 1 — colisión de `id`.** El `id` se arma con `(tipo, categoria, subcategoria, persona, moneda)`
> y NO incluye `tarjetaCodigo`. Las 8 filas de pago de tarjeta (4 tarjetas × 2 monedas) comparten
> esa tupla → colapsan a 2 docs (1 ARS, 1 USD). Confirmado: 23 filas activas → 17 ids únicos.
> Síntoma: solo 2 líneas "Pago Tarjeta" en el checklist; MC Black BBVA y MC Black Galicia sin esperado.
>
> **Bug 2 — match por `subcategoria` exacta.** La Rama 2 del matcher exige `categoria`+`subcategoria`
> iguales, pero el subcat de los movimientos es inconsistente (AySA vs Agua, "Internet y telefonía"
> vs Internet, "Colegio Federico" vs "Colegio Fede"). Esos esperados nunca matchean.

## Regla de trabajo
Antes de escribir, confirmá los 5 puntos de toque y esperá OK:
1. `src/types/index.ts` — `ExpectedItem.matchTexto: string | null` (línea ~76).
2. `scripts/seed/transformers/expectedItems.ts` — `buildItem` (el `id` se calcula ANTES de `matchTexto`) + `MATCH_TEXTO_OVERRIDES`.
3. `src/vistas/Resumen.tsx` — `movimientosDelItem`, Rama 2.
4. `src/datos/itemsEsperados.ts` — `docAItemEsperado` (mapea `matchTexto`).
5. `scripts/seed/validators/checks.ts` — agregar check anti-colisión.

---

## Cambios

### 1. Tipo `matchTexto` → objeto incluye/excluye
En `src/types/index.ts`:
```ts
export interface MatchTexto { incluye: string[]; excluye: string[]; }
```
Y en `ExpectedItem`: `matchTexto: MatchTexto | null;`

### 2. `expectedItems.ts` — id sin colisión + overrides
- **Reordenar `buildItem`**: calcular `tarjetaCodigo` y `matchTexto` ANTES del `id`, y agregar ambos al hash:
  ```ts
  const id = sha256Hex(
    'exp', tipo,
    r.Categoria ?? r['Categoría'] ?? '',
    r.Subcategoria ?? '',
    persona,
    r.Moneda ?? 'ARS',
    tarjetaCodigo ?? '',
    matchTexto ? matchTexto.incluye.join('|') : '',
  ).slice(0, 20);
  ```
- **`MATCH_TEXTO_OVERRIDES`**: pasa a llevar `{ incluye, excluye }` y se busca por `(tipo, categoria, subcategoria, persona)` (incluir `categoria`, porque ABL Cochera/Del Signo comparten subcat 'ABL' y difieren solo en categoria):

  | tipo | categoria | subcategoria | persona | incluye | excluye |
  |---|---|---|---|---|---|
  | Gasto | Auto | Cochera | (—) | `signo 2`, `depto 030`, `cochera` | — |
  | Gasto | Casa | Agua | (—) | `aysa`, `agua` | `signo 2`, `depto 030`, `cochera` |
  | Gasto | Auto | ABL | (—) | `abl cochera` | — |
  | Gasto | Casa | ABL | (—) | `abl` | `cochera` |
  | Gasto | Educación y chicos | Colegio Fede | Federico | `colegio federico`, `philips` | — |
  | Gasto | Casa | Internet | (—) | `internet` | — |
  | Gasto | Educación y chicos | Actividades extracurriculares | Federico | `micro rugby` | — |

  (la última es la migración del override de rugby que ya existía).
- El doc guarda `matchTexto` como objeto o `null`. Strings en minúsculas.
- Actualizar el log: contar `docs.filter(d => d.matchTexto)`.

### 3. `Resumen.tsx` — `movimientosDelItem`, Rama 2
Cuando el item tiene `matchTexto`, el match va por texto y **relaja** categoria/subcategoria
(quedan duros solo `tipo` + `moneda`). Si no tiene `matchTexto`, queda la lógica actual.
```ts
// Rama 2: matchTexto manda (relaja cat/subcat) — o, sin matchTexto, clave cat+subcat
return movs.filter(m => {
  if (m.tipo !== item.tipo) return false;
  if (m.moneda !== item.moneda) return false;
  if (item.matchTexto) {
    const desc = (m.descripcion ?? '').toLowerCase();
    const inc = item.matchTexto.incluye.some(t => desc.includes(t));
    const exc = item.matchTexto.excluye.some(t => desc.includes(t));
    return inc && !exc;
  }
  if (item.categoria    !== null && m.categoria    !== item.categoria)    return false;
  if (item.subcategoria !== null && m.subcategoria !== item.subcategoria) return false;
  if (item.tipo === 'Ingreso' && item.persona !== null && m.persona !== item.persona) return false;
  return true;
});
```
(Rama 0 itemEsperadoId y Rama 1 tarjeta por código quedan igual.)

### 4. `itemsEsperados.ts` — loader
`matchTexto: data.matchTexto ?? null` ya sirve (ahora es objeto). Si querés blindar forma:
`matchTexto: data.matchTexto ? { incluye: data.matchTexto.incluye ?? [], excluye: data.matchTexto.excluye ?? [] } : null`.

### 5. Validator — check anti-colisión (nuevo)
En `checks.ts`: `count(itemsEsperados) == filas esperadas activas en el Excel` (gastos+ingresos con `Activo`).
Hoy daría 17 vs 23 → debe pasar a 23 == 23 tras el fix. Esto detecta cualquier colisión futura de id.

### 6. Ciclo de seed limpio
`rmdir /s /q emulator-data` → emulators → `npm run seed` → `npm run validate`.
Objetivo: verde, con el check nuevo en 23 == 23.

---

## Definición de hecho (verificar en `/resumen`, mes junio 2026)
- 8 líneas "Pago Tarjeta" (4 tarjetas × 2 monedas); MC Black BBVA y MC Black Galicia ya con su propia línea/registro.
- Casa › Agua **registrado** con la AySA depto ("signo 1", $46.991); las `WWW.AYSA.COM.AR` caen acá.
- Auto › Cochera **registrado** con la AySA cochera ("Signo 2", $21.191), sin robarle a Del Signo.
- Casa › Internet **registrado** ("Personal - Internet...", $143.794).
- Educación › Colegio Fede **registrado** ("Escuela Philips... Federico", $1.271.500).
- ABL Del Signo / ABL Cochera / Gas **siguen pendientes** (no se pagaron en junio — correcto, no es bug).

## Nota
Tras el fix, las tarjetas no usadas en un mes figurarán pendientes. Si alguna no se usa nunca,
marcar `Activo=false` en la planilla en vez de tocar código.
