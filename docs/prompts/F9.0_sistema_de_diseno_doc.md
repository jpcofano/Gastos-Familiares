# F9.0 — Decisiones de diseño de la nueva etapa (DOC-ONLY)

**Objetivo:** documentar en `docs/CLAUDE.md` las decisiones de diseño de la etapa F9
(rediseño visual de la app sobre el design system `src/design-system/ui_kits/mobile`).
**No se escribe código.** Esta fase fija el contrato — diseño moderno vs. paridad
funcional con la legacy, plataforma primaria, color/tipografía/superficies/nav/íconos,
formato de montos — antes de tocar ningún componente o pantalla.

**Regla del proyecto:** mostrar el diff de `CLAUDE.md` antes de aplicarlo. No tocar
ningún `.tsx`, `.ts`, `firestore.rules` ni nada fuera de `docs/CLAUDE.md`.

---

## Cambio 1 — agregar a la sección `## Decisiones cerradas`

Insertado al final de la lista existente (después de la decisión sobre `/autorizados`):

```
- Diseño = app MODERNA (2026), no copia de la legacy. La legacy aporta funcionalidad, no
  estética; no arrastrar decisiones visuales impuestas por Apps Script.
- Funcionalidad = paridad con la legacy (sobre todo Dashboard, ver 60_Dash.gs).
- Plataforma primaria: móvil. Bottom-nav fijo: Inicio · Resumen · Cargar · Perfil.
  Captura (Alta manual, Confirmar comprobante) como modal full-screen.
- Color: marca/acción = esmeralda #065f46; ink #111827 para superficie oscura (hero de
  captura = primer paso del futuro tema oscuro). Azul = solo informativo; navy del rebuild
  deprecado. Semántica de plata: ingreso verde / gasto rojo (PENDIENTE: ajustar los tonos
  exactos de verde/rojo, ver F9.0b).
- Montos SIEMPRE mostrados en ARS-equivalente Y USD-equivalente (ambos), con tabular-nums,
  formato es-AR ($ 1.234,56 / U$S 1.234,56).
- Tema claro/oscuro: pendiente, se hará cuando estén todas las pantallas.
```

---

## Cambio 2 — nueva sección, insertada antes de `## Estructura del repo`

```
## Sistema de diseño / UI

Fuente: src/design-system/ui_kits/mobile (paquete del DS, copiado al repo; ver README.md
ahí dentro). App MODERNA 2026, no copia visual de la legacy — paridad de funcionalidad,
no de estética.

- Color: esmeralda (marca/acción) + ink (superficie oscura); azul solo informativo;
  navy deprecado; ingreso verde / gasto rojo (tonos finales pendientes, ver F9.0b).
- Tipografía: system UI stack, sin webfonts; escala de --text-2xs a --text-3xl;
  tabular-nums en números.
- Superficies: cards elevadas, radios generosos (18-22px), modal de captura full-screen
  (hero ink + drawer + CTA bar).
- Nav: bottom-nav fijo de 4 destinos con pill activo + FAB "+" flotante.
- Íconos: Lucide.
- Montos: siempre ARS-eq Y USD-eq, formato es-AR, tabular-nums.
- Tema claro/oscuro: pendiente hasta F9.3.
```

---

## Criterio de cierre

- `docs/CLAUDE.md` contiene los dos bloques, con el diff mostrado y aprobado antes de
  aplicar.
- Cero cambios fuera de `docs/CLAUDE.md` (y este mismo prompt en `docs/prompts/`).
- No se renumera ninguna decisión ni addendum existente.
