# F9.3 — Pantallas, una por PR (visual → datos)

> Pegá este mensaje en Claude Code.

```
Tarea: implementá las pantallas siguiendo el UI kit (ui_kits/mobile), UNA POR PR, en este
orden: Dashboard (Inicio) → Resumen → Cargar → Perfil + sub-pantallas (Miembros, Pagos
esperados, Categorías, Tipo de cambio) → Tarjetas → modales (Confirmar comprobante, Alta
manual).

Por cada pantalla: PRIMERO maquetá el visual con datos de ejemplo (sin tocar Firestore ni
Functions), en una PR; DESPUÉS, en otra PR, cableá los datos reales.

Requisitos transversales (de la auditoría F9.0b):
- TODOS los montos en ARS-eq Y USD-eq.
- Dashboard: paridad con 60_Dash.gs — neto/ingresos/gastos en eq, pesos disponibles,
  faltante USD, TC ref, gastos por categoría, por descripción, e histórico (gastos por mes /
  anual por categoría).
- Resumen: además del checklist de esperados, la vista de "pagos por día" de la legacy.

Regla: en las PRs visuales NO tocar firestore.rules, Cloud Functions ni lógica de datos.
Diff + aprobación en cada PR.
```
