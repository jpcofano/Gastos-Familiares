# F9.82 — Conciliación pago↔obligación: pase débil + picker "Conciliar con gasto esperado" + alias-learning

## Contexto
Caso real (julio): factura AYSA cochera ($22.208,61 · 1º venc; 2º venc $22.498,48) cargada como obligación. El pago BBVA/PagoMisCuentas ($22.498,48, "Pagaste a AYSA", **sin CUIT/CBU/alias**) NO reconcilió por rama 1 y cayó a rama 2 → movimiento nuevo → **gasto duplicado** ($44.707 en Dashboard).

Dos bloqueos independientes en `reconciliarPorPayee` (matchLogica.ts):
1. Corta con `if (!pCuit && !pCbu && !pAlias) return []` — nunca mira `destinoNombre`/`comercioRazonSocial`.
2. Monto exacto solo contra `m.monto` (1º venc) — ignora `m.vencimientos[]` (el 2º venc estaba ahí, propagado por F6.8 add.2).

## Principio (mantener)
- **Auto-confirma SOLO llave fuerte**: payee por CUIT/CBU/alias + monto exacto (ahora también contra vencimientos). Igual que hoy.
- **Pase débil (por nombre) NUNCA auto-confirma**: siempre rama 1 `candidatos`, aunque haya 1 solo. Consistente con F6.9.1.
- Rama 1 sigue **admin-only** (F6.9.11).

## ⚠️ Drift
Estos BUSCAR se escribieron contra `f88b5cf`. Desde entonces se aplicaron F9.77–F9.81. **Verificar cada bloque contra el código actual antes de aplicar**; si driftó, adaptar preservando la intención. Confirmar estado real de origin (`git ls-remote` / `git log origin/main -5`) antes de empezar.

---

## Cambio 1 — `functions/src/matchLogica.ts`: monto contra vencimientos + pase débil

### 1a. Helper de monto (fuerte y débil lo comparten)
Agregar cerca de `reconciliarPorPayee`:

```ts
// F9.82 — un pago puede saldar el monto cabecera O cualquier vencimiento (1º/2º venc)
function montoSalda(m: MovimientoMin, montoTotal: number): boolean {
  if (Math.abs(m.monto - montoTotal) < 0.01) return true;
  return (m.vencimientos ?? []).some(
    v => typeof v?.monto === 'number' && Math.abs(v.monto - montoTotal) < 0.01,
  );
}
```

Requiere sumar `vencimientos?: { monto?: number | null }[] | null` a `MovimientoMin` (y poblarlo en el mapeo de `movs` en `matchComprobante`, index.ts — hoy no se lee ese campo del doc).

### 1b. En `reconciliarPorPayee`, reemplazar la comparación de monto
BUSCAR:
```ts
    return Math.abs(m.monto - (datos.montoTotal as number)) < 0.01;
```
REEMPLAZAR:
```ts
    return montoSalda(m, datos.montoTotal as number);
```

### 1c. Pase débil por nombre — nueva función exportada
```ts
// F9.82 — pase débil: payee por nombre normalizado cuando no hay CUIT/CBU/alias.
// NUNCA auto-confirma: el caller siempre lo baja a rama 1 `candidatos`.
export function reconciliarPorNombre(
  datos: DatosExtractosMin,
  movs: MovimientoMin[],
  nombresDestinoAprendidos: Map<string, string> | null, // norm(nombre) → itemEsperadoId (de /destinos)
): MovimientoMin[] {
  const norm = (s: string | null | undefined) =>
    (s ?? '').trim().toLowerCase().replace(/\s+/g, ' ');
  const pNombre = norm(datos.destinoNombre) || norm(datos.comercioRazonSocial);
  if (!pNombre || datos.montoTotal == null) return [];

  const itemAprendido = nombresDestinoAprendidos?.get(pNombre) ?? null;

  return movs.filter(m => {
    if (m.tipo !== 'Gasto')        return false;
    if (m.moneda !== datos.moneda) return false;
    if (m.confirmadoPago)          return false;
    if (!montoSalda(m, datos.montoTotal as number)) return false;
    const mNombre = norm(m.destinoNombre);
    const nombreCoincide = !!mNombre &&
      (mNombre === pNombre || mNombre.includes(pNombre) || pNombre.includes(mNombre));
    const itemCoincide = !!itemAprendido && m.itemEsperadoId === itemAprendido;
    return nombreCoincide || itemCoincide;
  });
}
```
Requiere sumar `destinoNombre?: string | null` a `MovimientoMin` si no está (verificar — el mapeo en index.ts lee destinoCuit/Cbu/Alias pero chequear nombre).

## Cambio 2 — `functions/src/index.ts`: orquestación en `matchComprobante`

En el bloque `esPago` (F6.9), después del pase fuerte con 0 candidatos (donde hoy dice `// 0 candidatos → cae a clasificación normal`):

```ts
      // F9.82 — pase débil por nombre: propone, nunca auto-confirma
      const nombresAprendidos = await cargarNombresDestinoAprendidos(); // helper: query /destinos tipo=='nombre' con itemEsperadoId, confianza>=0.7, limit razonable
      const reconcDebil = reconciliarPorNombre(datos, movs, nombresAprendidos);
      if (reconcDebil.length > 0) {
        await ref.update({
          propuestaMatch: {
            rama: 1,
            candidatos: reconcDebil.map(m => ({
              tipo: 'movimiento' as const,
              id: m.id, score: 0,
              descripcion: m.descripcion, monto: m.monto, moneda: m.moneda,
              fecha: m.fecha.toISOString().slice(0, 10),
            })),
            origenReconciliacion: true,
            reconciliacionDebil: true, // F9.82 — flag para UI (copy distinto opcional)
            calculadoEn: FieldValue.serverTimestamp(),
          },
          actualizadoEn: FieldValue.serverTimestamp(),
        });
        console.log(`[matchComprobante] ${hashActual} → rama 1 débil (nombre, ${reconcDebil.length} candidatos)`);
        return;
      }
```

`reconciliacionDebil` se tipa en ambos gemelos `PropuestaMatch` (functions + cliente). El mapeo de `movs` debe sumar `vencimientos` y `destinoNombre` (ver 1a/1c).

## Cambio 3 — `src/datos/comprobantes.ts`: fix `?? null` en `confirmarRama1` (P1)

Hoy cada conciliación **pisa con null** el payee del emisor si el pago no lo trae (el BBVA no trae CUIT → borra el CUIT de la obligación → mata el aprendizaje).

BUSCAR (dentro del `batch.update` del movimiento):
```ts
      destinoCbu:    comp.datosExtraidos?.destinoCbu    ?? null,
      destinoCuit:   comp.datosExtraidos?.destinoCuit   ?? null,
      destinoAlias:  comp.datosExtraidos?.destinoAlias  ?? null,
      destinoNombre: comp.datosExtraidos?.destinoNombre ?? null,
      vencimientos:  comp.datosExtraidos?.vencimientos  ?? null,
```
REEMPLAZAR (merge conservador — solo escribir lo que el pago realmente trae):
```ts
      // F9.82 — no pisar payee del emisor con null: el pago suele traer menos datos que la factura
      ...(comp.datosExtraidos?.destinoCbu    ? { destinoCbu:    comp.datosExtraidos.destinoCbu }    : {}),
      ...(comp.datosExtraidos?.destinoCuit   ? { destinoCuit:   comp.datosExtraidos.destinoCuit }   : {}),
      ...(comp.datosExtraidos?.destinoAlias  ? { destinoAlias:  comp.datosExtraidos.destinoAlias }  : {}),
      ...(comp.datosExtraidos?.destinoNombre ? { destinoNombre: comp.datosExtraidos.destinoNombre } : {}),
      ...(comp.datosExtraidos?.vencimientos?.length ? { vencimientos: comp.datosExtraidos.vencimientos } : {}),
```

## Cambio 4 — alias-learning al conciliar

Al confirmar rama 1 (fuerte, débil o manual), aprender el **nombre del pago** como destino apuntando al mismo item, sin tocar el destino fuerte del emisor.

Opción recomendada (server, robusta): en `aprenderDestino` / su trigger, cuando el update del movimiento provenga de una conciliación (heurística: llega `hashPdf` nuevo + `confirmadoPago:true` + tiene `itemEsperadoId`), hacer un **upsert adicional** en `/destinos` con `normalizarDestino(nombreDelPago)` → `{ itemEsperadoId, categoria, confianza: 0.8 }`. El nombre del pago viaja en el update gracias al Cambio 3 (solo si el pago lo trae — el BBVA trae "AYSA" ✓).

Nota: hoy `aprenderDestino` aprende **una sola llave** (`cbu ?? cuit ?? alias ?? nombre`); este upsert es adicional y NO reemplaza esa lógica.

## Cambio 5 — UI: picker "Conciliar con gasto esperado" (`Comprobantes.tsx` + `comprobantes.ts`)

Para pagos (`esPago`) cuya propuesta cayó a rama 2/3, y **solo admin**:

- Nueva acción en la card: **"Conciliar con gasto esperado"**.
- Abre picker con los **itemsEsperados del mes** en estado no-pagado. Por cada uno, lookup reverse del FK: `movimientos where itemEsperadoId == X && confirmadoPago == false && mes == mesActual` (bounded, limit 5).
  - Item **con** movimiento-obligación abierto → al elegir: `confirmarRama1(comp, movId, itemId)` → **no crea movimiento**.
  - Item **sin** movimiento (pendiente puro) → al elegir: flujo actual `cargarMovimientoDesdeComprobante` con `itemEsperadoId` prefijado (rama 2 legítima, crea el único movimiento).
- Candidatos débiles (Cambio 2) reusan la UI de candidatos rama 1 existente; con `reconciliacionDebil` el copy puede decir "posible pago de factura" (opcional).
- El dependiente no ve el picker (rama 1 admin-only, F6.9.11) — su camino actual queda intacto.

## NO tocar
- `clasificador.ts`, `resumen_tarjeta`, `AltaMovimiento` (salvo el prefill de itemEsperadoId si el picker lo rutea por ahí).
- Auto-confirmación del pase fuerte: sin cambios de criterio.
- Reglas de Firestore.
- NADA de tolerancia flexible de monto ni auto-confirmación por nombre con confianza — fuera de scope (futuro).

## Gemelos a sincronizar
- `matchLogica.ts` ↔ `checklist.ts` (si el cliente replica lógica de reconciliación — verificar).
- `PropuestaMatch` tipo: functions ↔ cliente (`reconciliacionDebil`).
- `esObligacionDoc` sin cambios (ya gemelado, F9.75).

## Deploy
`cd functions && npm run build` → `firebase deploy --only functions,hosting`.

## Test end-to-end (con el caso real)
1. **Pre-limpieza (manual, Juan)**: borrar el movimiento duplicado del pago AYSA cochera de julio (dejar solo el movimiento-obligación de la factura, abierto). Guardar ids antes por las dudas.
2. Re-subir (o re-procesar) el comprobante BBVA → debe caer en rama 1 **débil** con la obligación como candidato (nombre "AYSA" ⊂ "Agua y Saneamientos…" no matchea por includes — acá valida el camino picker: conciliar manual).
3. Conciliar → verificar: un solo movimiento, `confirmadoPago:true`, CUIT del emisor **intacto** (Cambio 3), Dashboard Auto>Agua = $22.208,61 o $22.498,48 según definición de monto (ver pendiente abajo), no $44.707.
4. Verificar alias aprendido: `/destinos` tiene doc para "aysa" con `itemEsperadoId`.
5. Mes próximo: pago AYSA → candidato automático vía alias (pase débil con `/destinos`).

## Pendientes que este prompt NO resuelve (anotar)
- Monto del movimiento al conciliar 2º venc: la obligación quedó con $22.208,61 pero se pagó $22.498,48 — ¿`confirmarRama1` debería actualizar `monto` al del pago? Decisión de Juan, prompt aparte si va.
- Limpieza de duplicados históricos existentes (es data, no código).
- "AYSA" vs "Agua y Saneamientos Argentinos S.A." no matchea ni por includes — la primera vez siempre será picker manual; el alias-learning es lo que cierra el gap para las siguientes. Alternativa futura: reglasNormalizacion.

## Numeración
`F9.82` en filename, heading y ledger CLAUDE.md — actualizar CLAUDE.md en el mismo commit.
