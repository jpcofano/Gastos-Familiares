# F9.70 — Header global: avatar · luna (tema) · campana (vencimientos)

> Header/topbar de la app (`src/AppShell.tsx` y el `AppBar`/topbar que usan las vistas). Patrón **global** en todas
> las pantallas. Referencia: `ui_kits/mobile/index.html` → cluster `globalRight` (avatar + themeBtn + bell). Solo front.

---

## Objetivo

Cluster fijo a la derecha del header, en **todas** las pantallas, en este orden:
**avatar · luna · campana** (luna al medio, campana a la derecha).

- **Avatar** — monograma del miembro logueado (inicial sobre círculo `--gf-ink`); tap → Perfil.
- **Luna** — toggle de tema (el que ya existe); queda en el **medio**.
- **Campana** — ícono `bell` con **badge** de vencimientos próximos (mismo conteo que hoy alimenta el badge de
  Perfil › Notificaciones: esperados no pagados + tarjetas con `venceDia` ≤ hoy). Tap → Perfil › Notificaciones.

En Resumen, el botón "Tarjetas" que ya existe va **antes** del cluster (izquierda del avatar).

## Markup (portar de `globalRight` del kit)

```tsx
// avatar
<button onClick={irAPerfil} aria-label="Perfil" style={{ width:34,height:34,borderRadius:'50%',border:'none',cursor:'pointer',background:'var(--gf-ink)',color:'#fff',fontSize:14,fontWeight:700,flexShrink:0 }}>{inicial}</button>
// luna = el themeBtn actual
// campana
<button onClick={irANotificaciones} aria-label="Notificaciones" style={{ position:'relative',width:34,height:34,borderRadius:999,border:'none',background:'var(--gf-gray-100)',color:'var(--color-text)',display:'inline-flex',alignItems:'center',justifyContent:'center',cursor:'pointer',flexShrink:0 }}>
  <Icon name="bell" size={18} />
  {vencN > 0 && <span style={{ position:'absolute',top:-2,right:-2,minWidth:16,height:16,borderRadius:999,background:'var(--gf-expense)',color:'#fff',fontSize:10,fontWeight:700,display:'inline-flex',alignItems:'center',justifyContent:'center',padding:'0 4px',border:'2px solid var(--color-surface)' }}>{vencN}</span>}
</button>
```
Envolver en `<div style={{ display:'flex', alignItems:'center', gap:8 }}>`.

## Notas
- El conteo de vencimientos ya se calcula para el badge de Notificaciones — reusar esa fuente (no recalcular distinto).
- **Perfil → Patrimonio**: el rename de la tab y su gating (solo `jpcofano@gmail.com`) se maneja en otra tarea; acá el
  avatar abre Perfil como está hoy. No tocar el bottom-nav ni auth en este prompt.

## Test
1. Avatar + luna + campana visibles en Inicio, Resumen, Cargar, Perfil y sub-pantallas.
2. Campana muestra el nº de vencimientos y abre Notificaciones; avatar abre Perfil; luna cambia tema.
3. En Resumen, "Tarjetas" queda a la izquierda del cluster.

## Documentar
- `docs/CLAUDE.md`: registrar F9.70 (header global avatar·luna·campana; campana→Notificaciones con badge de vencimientos).
- Este archivo en `docs/prompts/`.
