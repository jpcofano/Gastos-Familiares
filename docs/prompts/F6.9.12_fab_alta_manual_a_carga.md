# F6.9.12 — FAB "+" de alta manual: del Dashboard a Carga (entrada única)

Hoy el alta manual tiene dos entradas dispersas: el FAB "+" flotante en el Dashboard
(`AltaMovimiento` **sin** `esManual`) y un botón inline punteado en Carga
(`preload={{ esManual: true }}`, que sí activa `numeroComprobante` + dedup
`existeNumeroComprobante`). Consolidar en **una sola** entrada: FAB "+" flotante en
**Carga**, con `esManual:true` (la versión correcta, con dedup). Sacar el FAB del Dashboard
y el botón inline de Carga.

Independiente de F6.9.11 — se puede aplicar/commitear por separado.

## Regla de oro (hacer primero)
Clone fresco. Confirmar líneas: `Dashboard.tsx` FAB (~225-240) + estado `mostrarAlta` (~93)
+ import `AltaMovimiento` (~4); `Comprobantes.tsx` bloque `cmp-alta-manual` (~559-574) +
estado `mostrarAltaManual` (~480); `Dashboard.css` `.dash-fab` (~47-68).

---

## Cambio 1 — `src/vistas/Dashboard.tsx`: sacar el FAB

BUSCAR:
```
      {/* Botón flotante "+" */}
      <button
        className="dash-fab"
        onClick={() => setMostrarAlta(true)}
        aria-label="Nuevo movimiento"
      >
        +
      </button>

      {mostrarAlta && (
        <AltaMovimiento
          memberId={memberId}
          miembro={miembro}
          onGuardado={handleGuardado}
          onCancelar={() => setMostrarAlta(false)}
        />
      )}
```
REEMPLAZAR: (borrar — dejar vacío)

Luego limpiar lo que quede huérfano en `Dashboard.tsx`:
- `const [mostrarAlta, setMostrarAlta] = useState(false);` → remover.
- `import AltaMovimiento from './AltaMovimiento';` → remover si ya no se usa.
- `handleGuardado` → remover si quedó sin referencias (verificar antes; si se usa en otro
  lado, dejarlo).

---

## Cambio 2 — `src/vistas/Comprobantes.tsx`: reemplazar el inline por el render overlay

El estado `mostrarAltaManual` (~480) se reutiliza. `AltaMovimiento` ya se renderiza como
overlay (`alta-overlay`), así que flota solo; no necesita el contenedor inline.

BUSCAR:
```
      {/* ── Alta manual ────────────────────────────────────────────────── */}
      <div className="cmp-alta-manual">
        {!mostrarAltaManual ? (
          <button className="cmp-btn-manual" onClick={() => setMostrarAltaManual(true)}>
            Cargar movimiento manual
          </button>
        ) : (
          <AltaMovimiento
            memberId={memberId}
            miembro={miembro}
            preload={{ esManual: true }}
            onGuardado={() => setMostrarAltaManual(false)}
            onCancelar={() => setMostrarAltaManual(false)}
          />
        )}
      </div>
```
REEMPLAZAR:
```
      {/* ── Alta manual (overlay; se abre con el FAB +) ─────────────────── */}
      {mostrarAltaManual && (
        <AltaMovimiento
          memberId={memberId}
          miembro={miembro}
          preload={{ esManual: true }}
          onGuardado={() => setMostrarAltaManual(false)}
          onCancelar={() => setMostrarAltaManual(false)}
        />
      )}
```

## Cambio 3 — `src/vistas/Comprobantes.tsx`: agregar el FAB

Agregar el FAB flotante al final del return de la vista Carga, **antes** del `</div>` de cierre
del contenedor raíz (`cmp`). Va siempre visible:
```
      <button
        className="cmp-fab"
        onClick={() => setMostrarAltaManual(true)}
        aria-label="Nuevo movimiento manual"
      >
        +
      </button>
```
(No gatear por rol — el alta manual es "dato propio"; el dependiente también la usa.)

---

## Cambio 4 — CSS: mover la regla del FAB

Mover el bloque `.dash-fab` (`Dashboard.css:47-68`, incluyendo `:hover` y `:active`) a
`Comprobantes.css`, renombrado `.cmp-fab`. **Remover** `.dash-fab` de `Dashboard.css`.
Misma definición (no cambiar estilos):
```css
.cmp-fab {
  position: fixed;
  bottom: 1.5rem;
  right: 1.5rem;
  width: 3.2rem;
  height: 3.2rem;
  border-radius: 50%;
  border: none;
  background: #1e3a5f;
  color: #fff;
  font-size: 1.8rem;
  line-height: 1;
  cursor: pointer;
  /* …resto de propiedades del .dash-fab original (box-shadow, display, etc.) tal cual… */
}
.cmp-fab:hover  { background: #2a5080; }
.cmp-fab:active { transform: scale(0.94); }
```
Copiar **todas** las propiedades del `.dash-fab` original (las líneas 60-66 que no pegué acá:
box-shadow / display / align / justify, lo que tenga). El `.cmp-btn-manual` del inline queda
sin uso → remover su regla CSS si existe.

---

## Cambio 5 — `docs/CLAUDE.md`: changelog
Línea: alta manual unificada en un FAB "+" flotante en Carga (`esManual:true`, con dedup
`numeroComprobante`); se retira el FAB del Dashboard y el botón inline de Carga.

---

## Criterios de cierre
- `npm run build` verde, sin imports/vars huérfanas (Dashboard limpio).
- Dashboard: ya no hay FAB; el resto intacto.
- Carga: FAB "+" abajo a la derecha; al tocarlo abre el alta manual como overlay; al guardar,
  crea el movimiento (con `numeroComprobante` + dedup) y cierra. Ya no está el botón punteado.
- Dependiente: ve el FAB en Carga y puede cargar un gasto manual propio.

## Convenciones
Diffs BUSCAR/REEMPLAZAR antes de aplicar. CLAUDE.md en el mismo commit. Test verde antes de
commit. Reauditar `origin` post-push.
