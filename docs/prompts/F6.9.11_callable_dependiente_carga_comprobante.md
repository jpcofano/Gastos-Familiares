# F6.9.11 — Callable: el dependiente carga su propio comprobante como movimiento (atómico, owner-scoped)

Opción B decidida. El dependiente debe poder convertir **su propio** comprobante en
**su propio** movimiento. El bloqueo es que el guardado toca dos updates admin-only
(`comprobantes:94`, `movimientos:64`) y la UI no está gateada. Solución: mover la
escritura privilegiada a una **callable server-side** (Admin SDK, igual patrón que
`resolverEntranteAmbiguo`/`descartarEntrada`), scopeada por dueño, **atómica** (crea
movimiento + marca comprobante `vinculado` en un batch) e **idempotente** (precondición
`estado === 'extraido'` ⇒ reintento no duplica — clave, porque en el camino comprobante
NO corre `existeNumeroComprobante`).

Las reglas de Firestore **no se tocan**: siguen admin-only para el cliente; la callable
bypassa con Admin SDK. Cero loosening (respeta el historial de P0).

Rama 1 (saldar una obligación) **queda admin-only** por decisión: se gatea su UI.

## Regla de oro (hacer primero)
Clone fresco de `origin`. Confirmar líneas: `comprobantes.ts` (`confirmarRama1` ~148,
`marcarVinculado` ~158), `AltaMovimiento.tsx` (`ejecutarGuardar` ~158, payload ~183-205),
`Comprobantes.tsx` (auto-confirm rama 1 ~98, `onGuardado` ~277, botones rama 1 ~163),
`functions/src/index.ts` (callables ~1334+). Si F6.9.9/F6.9.10 aún no están commiteados,
validar números reales.

---

## Cambio 1 — `functions/src/index.ts`: nueva callable

Agregar (es código nuevo, no BUSCAR/REEMPLAZAR). Usar los **mismos imports ya presentes**
en el archivo para timestamps (`FieldValue.serverTimestamp()`, `Timestamp`) — verificar
cómo se hace en las escrituras server-side existentes (~1173) y replicar ese estilo.

```ts
export const cargarMovimientoDesdeComprobante = onCall(
  { region: 'southamerica-east1' },
  async (request) => {
    if (!request.auth) throw new HttpsError('unauthenticated', 'No autenticado');
    const email = request.auth.token.email?.toLowerCase();
    if (!email) throw new HttpsError('unauthenticated', 'Email no disponible');

    const autSnap = await db.collection('autorizados').doc(email).get();
    if (!autSnap.exists) throw new HttpsError('permission-denied', 'No autorizado');
    const aut = autSnap.data()!;
    const callerMemberId = aut.memberId as string | undefined;
    const esAdmin = aut.rol === 'admin';
    if (!callerMemberId) throw new HttpsError('permission-denied', 'Sin memberId');

    const { compId, payload } = (request.data ?? {}) as { compId?: string; payload?: any };
    if (!compId)  throw new HttpsError('invalid-argument', 'compId requerido');
    if (!payload || typeof payload !== 'object') throw new HttpsError('invalid-argument', 'payload requerido');

    // Comprobante: existe + estado extraido (idempotencia) + dueño o admin
    const compRef  = db.collection('comprobantes').doc(compId);
    const compSnap = await compRef.get();
    if (!compSnap.exists) throw new HttpsError('not-found', 'Comprobante no encontrado');
    const comp = compSnap.data()!;
    if (comp.estado !== 'extraido')
      throw new HttpsError('failed-precondition', `Estado inválido: ${String(comp.estado)}`);
    if (!esAdmin && comp.subidoPor !== callerMemberId)
      throw new HttpsError('permission-denied', 'No es tu comprobante');

    // Validación server-side (Admin SDK bypassa reglas → replicar invariantes del create)
    const { persona, creadoPor, tipo, moneda, monto, descripcion, fechaMs, mes } = payload;
    if (creadoPor !== callerMemberId)
      throw new HttpsError('permission-denied', 'creadoPor debe ser vos');
    if (!esAdmin && persona !== callerMemberId)
      throw new HttpsError('permission-denied', 'persona debe ser vos');
    if (!['Gasto', 'Ingreso'].includes(tipo))  throw new HttpsError('invalid-argument', 'tipo inválido');
    if (!['ARS', 'USD'].includes(moneda))       throw new HttpsError('invalid-argument', 'moneda inválida');
    if (typeof monto !== 'number' || !(monto > 0)) throw new HttpsError('invalid-argument', 'monto inválido');
    if (typeof descripcion !== 'string' || descripcion.length === 0)
      throw new HttpsError('invalid-argument', 'descripcion requerida');
    if (typeof fechaMs !== 'number' || !Number.isFinite(fechaMs))
      throw new HttpsError('invalid-argument', 'fecha inválida');
    if (typeof mes !== 'string' || !/^[0-9]{4}-[0-9]{2}$/.test(mes))
      throw new HttpsError('invalid-argument', 'mes inválido');

    const fecha  = Timestamp.fromMillis(fechaMs);
    const movRef = db.collection('movimientos').doc();
    const batch  = db.batch();

    batch.set(movRef, {
      fecha, mes,
      tipo, descripcion,
      descripcionOriginal: payload.descripcionOriginal ?? null,
      monto, moneda,
      tcUsdArs:          payload.tcUsdArs           ?? null,
      categoria:         payload.categoria,
      subcategoria:      payload.subcategoria,
      etiqueta:          payload.etiqueta           ?? null,
      banco:             payload.banco              ?? null,
      persona, creadoPor,
      subtipo: 'Manual', origen: 'Manual',
      excluirDash: false, pagado: true,
      incluirResumenMes: payload.incluirResumenMes  ?? true,
      itemEsperadoId:    payload.itemEsperadoId      ?? null,
      numeroComprobante: payload.numeroComprobante  ?? null,
      confirmadoPago:    payload.confirmadoPago      ?? false,
      hashPdf:           payload.hashPdf             ?? null,
      refStoragePdf:     payload.refStoragePdf       ?? null,
      destinoCbu:        payload.destinoCbu          ?? null,
      destinoCuit:       payload.destinoCuit         ?? null,
      destinoAlias:      payload.destinoAlias        ?? null,
      destinoNombre:     payload.destinoNombre       ?? null,
      vencimientos:      payload.vencimientos        ?? null,
      origenComprobanteId: payload.origenComprobanteId ?? compId,
      creadoEn:      FieldValue.serverTimestamp(),
      actualizadoEn: FieldValue.serverTimestamp(),
    });
    batch.update(compRef, {
      estado: 'vinculado',
      actualizadoEn: FieldValue.serverTimestamp(),
    });
    await batch.commit();

    return { movimientoId: movRef.id };
  },
);
```

Notas:
- `mes` y `fechaMs` los manda el cliente (ver Cambio 3). `fechaMs` = ms de la fecha del form
  a mediodía local; derivar `mes` en el cliente evita drift de TZ (mediodía-AR nunca cruza
  día). El server valida el regex de `mes`; no lo recomputa.
- El shape del doc replica `crearMovimiento` (incluido `subtipo:'Manual'`/`origen:'Manual'`
  tal cual están hoy — mantener paridad, no "mejorar" acá).

---

## Cambio 2 — `src/datos/comprobantes.ts` (o `movimientos.ts`): wrapper cliente

Agregar fn que invoca la callable:
```ts
import { getFunctions, httpsCallable } from 'firebase/functions';
// usar la instancia/region que ya usás para las otras callables del proyecto

export async function cargarMovimientoDesdeComprobante(
  compId: string,
  payload: Record<string, unknown>,
): Promise<Resultado<{ movimientoId: string }>> {
  try {
    const fn = httpsCallable(getFunctions(/* app, 'southamerica-east1' */), 'cargarMovimientoDesdeComprobante');
    const res = await fn({ compId, payload });
    return { ok: true, data: res.data as { movimientoId: string } };
  } catch (e) {
    return { ok: false, error: e instanceof Error ? e : new Error(String(e)) };
  }
}
```
Verificar cómo se inicializan las otras callables del cliente (región/app) y replicar.

---

## Cambio 3 — `src/vistas/AltaMovimiento.tsx`: modo "guardar externo"

Para que el camino comprobante use la callable (atómico) y el alta manual siga client-side.

Agregar prop opcional:
```ts
onGuardarPayload?: (payload: Parameters<typeof crearMovimiento>[0]) => Promise<{ ok: boolean; error?: Error }>;
```

En `ejecutarGuardar`, si `onGuardarPayload` está, usarlo en vez de `crearMovimiento`:
```ts
const ejecutarGuardar = useCallback(async (payload) => {
  setGuardando(true);
  const resultado = onGuardarPayload
    ? await onGuardarPayload(payload)
    : await crearMovimiento(payload);
  setGuardando(false);
  if (resultado.ok) onGuardado();
  else setErrorMsg(resultado.error?.message ?? 'Error al guardar');
}, [onGuardado, onGuardarPayload]);
```

En el armado del `payload` (handleSubmit), agregar los dos campos que necesita la callable
(no rompen el camino manual: `crearMovimiento` los ignora):
```ts
fechaMs: new Date(fecha + 'T12:00:00').getTime(),
mes:     `${new Date(fecha + 'T12:00:00').getFullYear()}-${String(new Date(fecha + 'T12:00:00').getMonth() + 1).padStart(2, '0')}`,
```

---

## Cambio 4 — `src/vistas/Comprobantes.tsx`: wirear el camino "movimiento nuevo" a la callable

En el `AltaMovimiento` que renderiza la **rama nuevo** (el del `onGuardado` que hoy llama
`marcarVinculado`, ~277), reemplazar por `onGuardarPayload` que llama la callable, y **sacar**
el `marcarVinculado` (la callable ya marca `vinculado` atómicamente):

BUSCAR:
```
          onGuardado={async () => {
            await marcarVinculado(comp.id);
            setMostrarAlta(false);
          }}
```
REEMPLAZAR:
```
          onGuardarPayload={async (payload) => {
            const res = await cargarMovimientoDesdeComprobante(comp.id, payload);
            return { ok: res.ok, error: res.ok ? undefined : res.error };
          }}
          onGuardado={() => setMostrarAlta(false)}
```
Importar `cargarMovimientoDesdeComprobante`. `marcarVinculado` puede quedar para ramas 2/3
admin si todavía se usa; si queda huérfano, removerlo del import.

---

## Cambio 5 — `src/vistas/Comprobantes.tsx`: gatear rama 1 a admin

Rama 1 (saldar obligación) es admin-only por decisión. Para no-admin no debe auto-disparar
ni mostrar botón de confirmar.

- Auto-confirm (useEffect ~98): agregar guard `if (!esAdmin) return;` al inicio (el card ya
  recibe `esAdmin`; si en ese scope no está, threadearlo).
- Botones de rama 1 ("Confirmar selección" / "Confirmar"): envolver en `esAdmin &&`.
- Para dependiente en rama 1: mostrar estado informativo sin acción
  (p. ej. "Coincide con una obligación — un admin la concilia"). No bloquea su flujo de
  movimiento nuevo, que va por el otro camino.

---

## Cambio 6 — `firestore.rules`: SIN cambios
Confirmar explícitamente que `comprobantes:94` y `movimientos:64` siguen `esAdmin()`.
La callable es la única vía privilegiada para el dependiente. Dejar constancia en el commit.

---

## Cambio 7 — `docs/CLAUDE.md`: changelog
Línea: callable `cargarMovimientoDesdeComprobante` (owner-scoped, Admin SDK, atómica
crear-movimiento + marcar-comprobante-vinculado, idempotente por precondición
`estado==='extraido'`) habilita que el dependiente cargue su propio comprobante como su
propio movimiento sin aflojar reglas; rama 1 (conciliación de obligaciones) queda admin-only
y se gatea en UI; `AltaMovimiento` gana `onGuardarPayload` para ruteo callable vs client.

---

## Criterios de cierre
- Build de functions verde (`cd functions && npm run build`) y front verde.
- Emulador, logueado **dependiente**: subir comprobante → extraer → "Revisar y cargar" →
  se crea su movimiento (`persona` = él) y el comprobante pasa a `vinculado`, en una sola
  operación. Sin "permission-denied".
- **Idempotencia:** reintentar la misma carga no duplica (segundo intento corta en
  `failed-precondition` porque ya está `vinculado`).
- **No-impersonación:** un payload con `persona`/`creadoPor` de otro miembro lo rechaza la
  callable (`permission-denied`).
- Dependiente **no** ve botón de confirmar en rama 1; admin sí, y admin sigue cargando normal.
- Admin: el camino comprobante→movimiento nuevo ahora también pasa por la callable (atómico);
  verificar que sigue funcionando igual.

## Convenciones
Emulator primero. Diffs BUSCAR/REEMPLAZAR antes de aplicar. CLAUDE.md en el mismo commit.
Test verde antes de commit. Reauditar `origin` post-push (`git ls-remote` +
`git log origin/main..HEAD --oneline`).
