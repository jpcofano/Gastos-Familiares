# F9.17 — Resumen: rediseño de color y forma (dirección "A + hero ink")

> Reglas: doc-first · una PR por fase · sin tocar Firestore/Functions · móvil primero.
> Aplica al tab **Por día** de Resumen. NO se quita información: solo cambia color/forma.

Dirección elegida: **A (limpio) + hero ink (de B)**. Saca los arrastres legacy:
demasiados fondos teñidos (verde + azul + amarillo) y el azul off-brand de "Juan".

## 1. "Neto del mes" → card ink (no más tinte esmeralda)
- Fondo `linear-gradient(135deg, var(--gf-ink) 0%, var(--gf-ink-soft) 100%)`, texto
  blanco, `radius-card`, `shadow-soft`. Rima con el "Balance" del Dashboard.
- Número del neto en **blanco** (el signo + el desglose comunican el resultado), U$S a
  la derecha en `rgba(255,255,255,.7)`. Divider `rgba(255,255,255,.15)`.

## 2. "Revisar pendientes" → badge con cantidad (no banner amarillo)
- Reemplazar el `Message kind="warn"` por una fila limpia: card surface + borde fino,
  ícono `alert-circle` en `var(--gf-out)`, texto "Revisar pendientes del mes", y a la
  derecha un **badge redondo con el número** de pendientes (fondo `--gf-out`, texto
  blanco). El número sale del conteo real de ítems por revisar.

## 3. Distribución de ingresos → sin fondos teñidos
- Las cards Juan/María pasan a **surface + borde `--gf-gray-150`** (antes: una azul
  `#eef4ff`, otra `emerald-50`). La identidad del miembro queda en el **punto de color**,
  no en el fondo. Nada de azul de fondo.

## 4. Una sola forma
- Cards con borde fino y radio consistente (16px; filas 14px). Sin acentos de
  borde-izquierdo. El estado se lee por punto de color + texto, no por fondo.

## Mantener intacto (no quitar)
- KPIs Disponible (ARS) / Resultado (USD).
- "Hoy — <fecha>" con su estado.
- **Gastos por día = tabla por día y por banco**: cada día lista sus bancos con su
  monto (chips con punto del color del banco) + total del día en ARS y U$S.
- **Gastos Fijos** sigue en su **propia solapa** (no mezclar con Por día).

PR visual (sin tocar datos ni Functions).
