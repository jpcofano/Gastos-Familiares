# F9.34 — Re-skin mobile de Cargar + Alta Manual (deuda visual de F9.26)

> Reglas: doc-first · una PR · móvil primero. **PR visual**: re-skin, NO toca la lógica de
> datos (que en F9.26 quedó real y probada, restaurada de git 6acf084). Solo lleva esas
> pantallas al lenguaje mobile del kit. Mismo patrón que F9.21/F9.22 (real primero, lindo
> después).

## Contexto
En F9.26, Cargar y `AltaMovimiento.tsx` se restauraron desde una implementación real y más
evolucionada (F6.8/F6.9: ramas de match, reconciliación por payee, scoping admin/dependiente,
descartar comprobante, FAB unificado). El costo aceptado: usan el **estilo pre-F9.3**
(overlay tipo desktop), no el lenguaje mobile del rediseño. Esta fase salda esa deuda **sin
tocar la lógica**.

## Referencia de diseño (kit)
`ui_kits/mobile/CargaMobile.jsx`, `ManualGasto.jsx`, `ComprobanteConfirm.jsx`, y el scaffold
de `MobileShell.jsx`: **`FullModal`** (overlay full-screen sobre `--gf-ink`), **`Hero`**
(eyebrow + monto + desc + tags), **`ModalBar`**, **`Drawer`**, **`CtaBar`** (acción fija
abajo), **`SectionLabel`**. El alta/captura es full-screen mobile, no un panel de escritorio.

## Tarea (solo presentación)
1. **Alta Manual** (`AltaMovimiento.tsx`): envolver en `FullModal` + `Hero` (monto en vivo
   arriba), campos en `Drawer`/secciones, confirmación en `CtaBar` fija. Reemplazar el
   overlay desktop. Form, validación, `crearMovimiento`, `tcParaFecha`, clasificador y
   memberId **intactos**.
2. **Cargar / Comprobantes**: dropzone + entrantes + confirmación con el lenguaje mobile
   (cards, hero de comprobante, estados ok/wait/warn como en el kit). La máquina de match y
   el scoping admin/dependiente **no cambian**.
3. **Confirmar comprobante**: usar el patrón de `ComprobanteConfirm.jsx` (Hero del
   comprobante + filas editables + CTA), sin alterar el enlace al movimiento real.
4. **Tema claro/oscuro** y tokens del kit (sin `#fff` hardcodeados; ver F9.19/F9.20).

## Aceptación
- Cargar y Alta Manual con el look mobile del kit (FullModal/Hero/CtaBar), no el overlay
  pre-F9.3.
- **Cero cambios de comportamiento**: match, reconciliación, scoping, descartar, FAB, alta
  con memberId y TC por fecha siguen funcionando igual (mismos tests/fltujos que F9.26).
- Sin regresión de datos; build verde.

## Notas
- Meter **antes** del gate de paridad F9.32 si se puede — si no, F9.32 lo va a marcar como
  faltante visual igual. Ver `CLAUDE.md` (deudas de F9.26).
