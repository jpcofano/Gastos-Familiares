# F4.1 — Router + capa de datos + Dashboard mínimo

**Fase:** F4 (vistas read-only) · **Sub-paso:** 4.1
**Prerequisito:** F4.0 cerrado (modelo 100% castellano, 8 OK / 0 FAIL, login funcionando).
**Scope:** solo emulador, solo LECTURA. Ninguna escritura a Firestore en toda la fase F4.

---

## Contexto

El shell tiene los 4 estados andando y un `<main className="shell-content">` como slot. Decisión cerrada: **React Router** (`react-router-dom`), rutas montadas dentro del slot, header fijo afuera.

Colecciones ya en castellano: `movimientos`, `resumenesTarjeta`, `itemsEsperados`, `diccionario`, `reglasNormalizacion`, `tcDiario`, `usuarios`, `config`, `subcategorias`, `etiquetas`.

Reglas del proyecto que aplican a TODA esta fase:
- **Toda query filtra por `mes` o usa `limit()`.** Nunca una query sin filtro temporal.
- **Lecturas one-shot** (`getDocs`/`getDoc`). El `onSnapshot` para la pantalla del mes es un compromiso posterior, NO entra en F4.1.
- El patrón de conversión Timestamp→Date ya existe en `docAFamiliaConfig` (F3.3) — generalizarlo acá.

---

## Antes de escribir — verificar y reportar

1. **`firestore.rules`:** ¿permiten a un usuario autenticado leer `movimientos`? Reportar la regla tal cual. Si no lo permiten, proponer el diff mínimo y **esperar aprobación**. (El scope fino por rol — hijos solo su persona — se valida en F4.3; acá alcanza con que un autenticado pueda leer.)
2. Confirmar qué campo usa `movimientos` para el mes (`mes: string` formato `YYYY-MM` según el tipo) y que existe índice utilizable para `where('mes','==',...)`.
3. **Esperar aprobación del plan antes de codear.**

---

## Qué hace F4.1

1. **Router:**
   - Instalar `react-router-dom`.
   - Rutas iniciales: `/` (Dashboard) y placeholder `/resumen` ("Próximamente"). Las rutas viven DENTRO del estado `autenticado` del shell; los otros 3 estados no tienen rutas.
   - Navegación mínima en el header (links Dashboard / Resumen).

2. **Capa de datos** (`src/datos/` o equivalente):
   - `movimientosDelMes(mes: string): Promise<Movement[]>` — `getDocs` con `where('mes','==',mes)`, conversión de cada doc a `Movement` del dominio (Timestamps→Date) en un mapper `docAMovimiento`, hermano del `docAFamiliaConfig` existente. Mapper por colección, en un solo lugar.
   - Manejo de error: si la query falla, devolver el error tipado hacia la vista, no tragar.

3. **Dashboard mínimo** (`/`):
   - Selector de mes (default: mes actual; navegación mes anterior/siguiente).
   - Totales del mes: ingresos, gastos, balance — separados por moneda (ARS / USD). NO convertir entre monedas todavía (el TC en vistas llega después).
   - Lista de movimientos del mes (fecha, descripción, categoría, monto con signo y moneda), ordenada por fecha desc. Virtualización NO necesaria (~100 movs/mes).
   - Estados de carga y de "mes sin movimientos".
   - Respetar `excluirDash`: los movimientos con `excluirDash === true` NO se muestran ni suman (es la semántica heredada del viejo: los TarjetaPago no van al Dashboard).

4. **Sin diferencias por rol todavía:** admin y dependiente ven lo mismo en F4.1. El filtro por persona para hijos es F4.3.

---

## Restricciones

- **NO** escrituras a Firestore.
- **NO** `onSnapshot`.
- **NO** queries sin filtro de mes.
- **NO** agregaciones server-side todavía (compromiso F4 posterior; en F4.1 sumar en cliente está bien porque ya está filtrado por mes).
- **NO** tocar `scripts/seed/`.

---

## Criterio de cierre

- Navegar a `/` muestra el Dashboard del mes actual con datos reales del emulador.
- Cambiar de mes recarga correctamente; un mes sin datos muestra el estado vacío.
- Totales ARS y USD correctos contra una verificación manual de 2-3 movimientos en el emulator UI.
- Movimientos con `excluirDash` no aparecen ni suman.
- Refresh en `/resumen` mantiene la ruta (deep link funciona).
- `npm run validate`: 8 OK / 0 FAIL.
