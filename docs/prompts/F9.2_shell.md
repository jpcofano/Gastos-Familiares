# F9.2 — Shell móvil (AppBar + BottomNav + modal de captura + Icon)

> Pegá este mensaje en Claude Code.

```
Tarea: implementá el shell móvil siguiendo el UI kit del design system (ui_kits/mobile):
- AppBar (título + subtítulo, con back opcional).
- BottomNav fijo de 4 destinos (Inicio · Resumen · Cargar · Perfil) con pill activo.
- Contenedor de pantalla scrolleable + FAB "+" para alta rápida.
- Modal full-screen de captura (hero ink + drawer + CTA bar).
- Componente Icon: wrapper de Lucide en React que evita el crash de createIcons() sobre
  nodos manejados por React (renderizá un <span> propio y dejá que Lucide reemplace un <i>
  interno; React nunca reconcilia el svg).

Solo estructura/navegación, sin cablear datos reales aún. Diff + aprobación.
```
