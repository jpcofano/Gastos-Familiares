# F9.137 — EL SEXTO Y SÉPTIMO CASO DEL PATRÓN DE F9.136

Te lo debía desde antes de F9.138. Mismo patrón: el estado usado como proxy de
"hay match".

## §1 — `informeMensual.ts:57`, mismo patrón, peor consecuencia

Un ítem con match que ahora es `'vencido'` cae al else y se clasifica `'pendiente'`
en vez de `'a confirmar'`. No queda en la etiqueta: `:74-77` cuenta `pendientes`
sobre ese bucket y suma `esperadoUsd` a `totalPendienteUsd`. Un ítem con plata
ya cargada entra al informe como pendiente y suma su monto ESPERADO, teniendo
`realUsd` calculado en `:60`.
Preguntá por `ci.matches.length`, no por el estado.
Revisá aparte si `totalPendienteUsd` debería usar `realUsd` cuando existe —
reportá cuánto cambia el número ANTES de decidirlo.
Se rompió en silencio porque F9.124 no está deployado. Eso lo hace peor, no
menos grave.

## §2 — `Notificaciones.tsx:76`, cosmético

`sub` dice "Pago detectado — falta confirmar" solo para `'por_confirmar'`. Un
vencido con match ya no avisa que el movimiento está cargado. Misma corrección.

## §3 — Barrido, ahora sobre TODO el repo

Los dos que faltaron en F9.136 estaban fuera de `src/vistas`. Repetí el barrido
de igualdades literales contra `EstadoChecklist` con tu propio criterio —el tipo
protege los `Record`, las igualdades sueltas no— sobre `src/` y `functions/`
enteros. Reportá el conteo final aunque dé cero adicionales.

## §4 — `Resumen.tsx:906`, el `?? 0`

Un esperado sin `montoEsperado` se rinde "$ 0": "no sé cuánto" impreso como
"cero". El criterio ya está escrito en `informeMensual.ts:64`, con el comentario
que lo explica. Mismo trato en la card: sin monto esperado no se imprime número.

## VERIFICACIÓN

- Informe mensual: cuántos ítems cambian de bucket y cuánto cambia
  `totalPendienteUsd`. Los dos números, antes y después.
- Ningún esperado sin monto muestra "$ 0".
- Barrido de §3 reportado.
- `tsc` 41/0. `vite build` OK.

No deployes. Avisame y deployo yo.
