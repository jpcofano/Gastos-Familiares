# F9.18 — Gastos Fijos: limpieza de color y forma (dirección A)

> Reglas: doc-first · una PR por fase · sin tocar Firestore/Functions · móvil primero.
> Aplica a la solapa **Gastos Fijos** de Resumen. NO se quita información.

Misma dirección que F9.17 (Por día): sacar el cliché legacy de **borde-izquierdo +
fondo pastel** en cada ítem.

## Cambios
- Cada ítem pasa de `fondo teñido por estado + borde-izquierdo 3px` a una **card surface
  + borde fino `--gf-gray-150`, radio 14**.
- El estado se comunica con un **punto de color** a la izquierda (color del estado) +
  el `StatusBadge` que ya existe — no con el fondo de toda la fila.
- Se mantienen: persona (badge), monto (con `decimals={0}`), nota "Falta una cuota" en
  estado parcial, y el botón **"Marcar pagado"** en ítems accionables.
- KPIs "Pendiente" / "Al día (n/total)" quedan igual.

## Nota
- El color del estado **"Por confirmar"** sigue siendo azul (es un color de estado del
  design system, no el azul de marca). Si se quiere unificar fuera del azul, es un cambio
  de la paleta de estados (StatusBadge) en fase aparte — no de esta PR.

PR visual (sin tocar datos ni Functions).
