# F9.58 — Banner de instalación PWA in-app + fix warnings de manifest

## Contexto

La PWA es instalable (manifest OK, SW activo y corriendo, confirmado en
desktop y Android/Chrome), pero el usuario depende de encontrar la opción
"Instalar app" en el menú del navegador. Necesitamos:

1. Un banner in-app que capture `beforeinstallprompt` y ofrezca instalar
   con un botón propio, en vez de depender del menú del navegador.
2. Sacar el warning de `<meta name="apple-mobile-web-app-capable">`
   deprecado (Chrome pide la versión nueva `mobile-web-app-capable`,
   manteniendo la vieja para iOS).
3. Sacar el warning de "Richer PWA Install UI" agregando `screenshots`
   al manifest.

No toca lógica de negocio. Cero impacto en Firestore/Functions.

---

## 1. `index.html` — meta tag nueva (mantener la vieja para iOS)

BUSCAR:
```html
    <meta name="theme-color" content="#1e3a5f" />
    <link rel="manifest" href="/manifest.webmanifest" />
    <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
```

REEMPLAZAR:
```html
    <meta name="theme-color" content="#1e3a5f" />
    <link rel="manifest" href="/manifest.webmanifest" />
    <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
    <meta name="mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
```

---

## 2. `public/manifest.webmanifest` — agregar `screenshots`

Richer Install UI de Chrome pide al menos un screenshot `form_factor:
"narrow"` (mobile) y uno `"wide"` (desktop) — o uno solo sin `form_factor`
declarado como wide-fallback. Sin esto Chrome ya instala igual, solo
muestra el diálogo simple; el campo es 100% cosmético pero saca el warning.

**Importante:** los archivos de imagen tienen que ser capturas reales de
la app (no se pueden inventar/generar — Chrome valida dimensiones pero el
contenido lo ve el usuario en el diálogo de instalación). Juan: sacá dos
capturas de pantalla reales —

- Mobile: pantalla de Inicio/Dashboard en el celu, recortada a algo tipo
  `1080x2280` (proporción de pantalla del Samsung)
- Desktop: la misma vista pero en el navegador de escritorio, `1920x1080`

y guardalas como:
```
public/screenshots/mobile-dashboard.png
public/screenshots/desktop-dashboard.png
```

BUSCAR (en `public/manifest.webmanifest`):
```json
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png" },
    { "src": "/icons/icon-maskable-512.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" }
  ],
```

REEMPLAZAR:
```json
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png" },
    { "src": "/icons/icon-maskable-512.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" }
  ],
  "screenshots": [
    {
      "src": "/screenshots/mobile-dashboard.png",
      "sizes": "1080x2280",
      "type": "image/png",
      "form_factor": "narrow"
    },
    {
      "src": "/screenshots/desktop-dashboard.png",
      "sizes": "1920x1080",
      "type": "image/png",
      "form_factor": "wide"
    }
  ],
```

Si Juan todavía no tiene las capturas a mano, este bloque se puede aplicar
después por separado — no bloquea el resto del prompt.

---

## 3. Hook nuevo — `src/hooks/useInstallPrompt.ts`

Encapsula la captura de `beforeinstallprompt`, el estado de "ya instalada"
(`display-mode: standalone`), y la función para disparar el prompt nativo.

CREAR archivo nuevo:
```ts
import { useEffect, useState, useCallback } from 'react';

interface BeforeInstallPromptEvent extends Event {
  prompt: () => Promise<void>;
  userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }>;
}

function yaInstalada(): boolean {
  return window.matchMedia('(display-mode: standalone)').matches
    || (window.navigator as any).standalone === true; // iOS Safari
}

export function useInstallPrompt() {
  const [deferredEvent, setDeferredEvent] = useState<BeforeInstallPromptEvent | null>(null);
  const [instalada, setInstalada] = useState(yaInstalada());
  const [descartado, setDescartado] = useState(
    () => sessionStorage.getItem('gf-install-banner-descartado') === '1'
  );

  useEffect(() => {
    if (instalada) return;

    const onBeforeInstall = (e: Event) => {
      e.preventDefault();
      setDeferredEvent(e as BeforeInstallPromptEvent);
    };
    const onInstalled = () => {
      setInstalada(true);
      setDeferredEvent(null);
    };

    window.addEventListener('beforeinstallprompt', onBeforeInstall);
    window.addEventListener('appinstalled', onInstalled);
    return () => {
      window.removeEventListener('beforeinstallprompt', onBeforeInstall);
      window.removeEventListener('appinstalled', onInstalled);
    };
  }, [instalada]);

  const instalar = useCallback(async () => {
    if (!deferredEvent) return;
    await deferredEvent.prompt();
    const { outcome } = await deferredEvent.userChoice;
    setDeferredEvent(null);
    if (outcome === 'accepted') setInstalada(true);
  }, [deferredEvent]);

  const descartar = useCallback(() => {
    setDescartado(true);
    sessionStorage.setItem('gf-install-banner-descartado', '1');
  }, []);

  const mostrarBanner = Boolean(deferredEvent) && !instalada && !descartado;

  return { mostrarBanner, instalar, descartar };
}
```

Nota: `sessionStorage` para que el descarte dure la sesión del tab, no para
siempre — si querés que sea permanente cambiá a `localStorage`, pero ojo
que entonces un usuario que descartó por error no lo vuelve a ver nunca
(salvo que borre datos del sitio).

---

## 4. Componente nuevo — `src/design-system/shell/InstallBanner.tsx`

Sigue el mismo patrón de estilo inline + tokens que `Fab.tsx`.

CREAR archivo nuevo:
```tsx
import { Icon } from '../Icon';
import { useInstallPrompt } from '../../hooks/useInstallPrompt';

export function InstallBanner() {
  const { mostrarBanner, instalar, descartar } = useInstallPrompt();

  if (!mostrarBanner) return null;

  return (
    <div
      style={{
        position: 'fixed', left: 12, right: 12, bottom: 76, zIndex: 6,
        background: 'var(--gf-ink)', color: '#fff',
        borderRadius: 16, padding: '12px 14px',
        display: 'flex', alignItems: 'center', gap: 12,
        boxShadow: '0 8px 24px rgba(0,0,0,.35)',
        fontFamily: 'var(--font-base)',
      }}
    >
      <div
        style={{
          width: 40, height: 40, borderRadius: 10, flexShrink: 0,
          background: 'var(--gf-emerald)', display: 'flex',
          alignItems: 'center', justifyContent: 'center',
        }}
      >
        <Icon name="download" size={20} />
      </div>
      <div style={{ flex: 1, minWidth: 0 }}>
        <p style={{ margin: 0, fontSize: 13, fontWeight: 600 }}>Instalá la app</p>
        <p style={{ margin: 0, fontSize: 12, opacity: .75 }}>
          Accedé más rápido y compartí comprobantes directo desde el celu.
        </p>
      </div>
      <button
        onClick={instalar}
        style={{
          background: 'var(--gf-emerald)', color: '#fff', border: 'none',
          borderRadius: 999, padding: '8px 14px', fontSize: 12, fontWeight: 600,
          cursor: 'pointer', flexShrink: 0, fontFamily: 'var(--font-base)',
        }}
      >
        Instalar
      </button>
      <button
        onClick={descartar}
        aria-label="Cerrar"
        style={{
          background: 'transparent', border: 'none', color: '#fff',
          opacity: .6, cursor: 'pointer', flexShrink: 0, padding: 4,
        }}
      >
        <Icon name="x" size={16} />
      </button>
    </div>
  );
}
```

Confirmar que `Icon` (en `src/design-system/Icon.tsx`) soporta los nombres
`download` y `x` de `lucide-react` — si no, ajustar a los nombres
equivalentes que ya use el resto de la app.

---

## 5. `src/design-system/shell/index.ts` — exportar el nuevo componente

BUSCAR (el barrel actual exporta `AppBar`, `Screen`, `BottomNav`, etc. —
agregar la línea siguiendo el mismo patrón):
```ts
export { Fab } from './Fab';
```

REEMPLAZAR:
```ts
export { Fab } from './Fab';
export { InstallBanner } from './InstallBanner';
```

(Si el archivo no tiene esa línea exacta, agregar el `export` nuevo al
final del barrel respetando el estilo existente.)

---

## 6. `src/AppShell.tsx` — montar el banner en `ShellFrame`

BUSCAR:
```tsx
import { AppBar, Screen, BottomNav, type BottomNavItem } from './design-system/shell';
```

REEMPLAZAR:
```tsx
import { AppBar, Screen, BottomNav, InstallBanner, type BottomNavItem } from './design-system/shell';
```

BUSCAR:
```tsx
      <Screen>
        <Routes>
```

REEMPLAZAR:
```tsx
      <Screen>
        <InstallBanner />
        <Routes>
```

(Se monta dentro de `<Screen>` para que el `position: fixed` tenga el
mismo stacking context que el resto del shell — mismo criterio que ya usa
`Fab`, que vive fuera de Routes pero dentro del flujo del shell-phone.)

---

## Checklist antes de commitear

- [ ] `npm run build` sin errores de TS
- [ ] Probar en Chrome desktop: el banner aparece cuando `beforeinstallprompt`
      dispara (puede no disparar si ya está instalada en esa máquina — usar
      perfil limpio o `chrome://apps` → desinstalar primero para testear)
- [ ] Probar en Chrome Android (Samsung): mismo chequeo, confirmar que el
      botón "Instalar" del banner dispara el diálogo nativo
- [ ] Confirmar que "Cerrar" (X) hace desaparecer el banner y no vuelve a
      aparecer en esa sesión
- [ ] Confirmar en Application → Manifest que el warning de
      `apple-mobile-web-app-capable` deprecado desapareció de la consola
- [ ] Si se agregaron los screenshots: confirmar que el warning "Richer PWA
      Install UI" desapareció de Application → Manifest
- [ ] Actualizar `docs/CLAUDE.md` si corresponde (nuevo hook + componente)
- [ ] Commit: `F9.58 -- banner de instalacion PWA in-app + fix warnings manifest`
