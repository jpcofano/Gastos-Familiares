# F9.160 — informe

Base `2cd2699` (F9.157), verificada. `tsc` cliente **41 → 41**, functions **0 → 0**;
`npm run build` y `functions build` OK. **Nada escrito en producción.**

---

## §1 — el segundo error: es un ajuste manual del dueño

Las cinco preguntas, contra `08a697e0`.

**1. ¿Qué subtotal no cierra, y por cuánto?** Solo MARÍA, por **−337.084,00** (= 2 × 168.542).
Los otros tres cierran al centavo, y el USD cierra en los cuatro.

```
sección       |  n |     suma parseada |        PDF |        Δ
FEDERICO      | 33 |      107712.15 |      107712.15 |           0.00
JUAN          | 21 |      807818.44 |      807818.44 |           0.00
MARIA         | 31 |      999813.92 |     1336897.92 |     -337084.00   <<< NO CIERRA
SOFIA         |  2 |        6951.93 |        6951.93 |           0.00
```

**2. ¿Faltan o sobran renglones?** No falta ninguno de detalle. Los que la spec nombra y no
están (`SU PAGO EN PESOS`, `SU PAGO EN USD`, `SALDO ANTERIOR`) **es correcto que no estén**:
cancelan contra el saldo anterior y no son renglones de consumo.

**3. `SU PAGO EN PESOS` / `SU PAGO EN USD`:** ausentes, y bien. No entran al cuadre con
ningún signo.

**4. `CR.RG 5617 30% M` y `DB.RG 5617 30%`:** el primero **no es una línea**, está en
`ajustesConsolidado` como `−890.257,63` (que es exactamente donde corresponde: el total del
PDF ya viene neto de él). El segundo sí es línea, `tipoLinea="impuesto"`, entra **sumando**
33.406,41, y coincide con el PDF.

**5. ¿Da cero por las razones correctas?** **Sí**, y hacen falta los dos cambios:

```
HOY (seguro como reintegro + ajuste manual)    sumaARS=1403821.10 diffARS=0.00 balanceARS=true
solo corrijo el seguro                         sumaARS=1740905.10 diffARS=337084.00 balanceARS=false
solo saco el ajuste manual                     sumaARS=1066737.10 diffARS=337084.00 balanceARS=false
LOS DOS: seguro consumo + sin ajuste manual    sumaARS=1403821.10 diffARS=0.00 balanceARS=true
```

**El segundo error es el parche.** No es un renglón mal parseado: es
`{"concepto":"Diferencia no identificada (ajuste manual)","montoARS":337084,"origen":"manual"}`,
que el dueño agregó con "cerrar diferencia" para tapar justo el descuadre que producía el
seguro. Los dos errores se cancelaban porque **el segundo se construyó para cancelar al
primero**.

**La causa del primero es el prompt, y era sistemática, no errática** — mi conclusión de
F9.157 estaba invertida. El prompt listaba `CAJA SEG-PROMO` entre los reintegros (línea
~1028) y además tenía un caso especial explícito (~1152) que reconocía el importe positivo y
**aun así** mandaba `reintegro_percepcion`.

---

## §2 — BLOQUEADO: el signo no se conserva. No construí el guard.

La spec pide auditar antes de construir y avisar si el dato se perdió. Se perdió:

```
líneas en los 30 resúmenes: 1810 | con monto NEGATIVO: 0
el crédito real del PDF "COTO DIGITAL SUC 056 CRED" (-8.870,44) está guardado como: 8870.44
campos de una línea: categoria, cuotaActual, cuotaTotal, descripcionRaw, esBonificacion,
  esImpuesto, esReverso, fechaConsumo, incluir, moneda, monto, nroCupon, personaConfirmada,
  personaDetectada, seq, subcategoria, tipoLinea
¿hay algún campo con el signo o la sección de origen? NO
```

Cero de 1810 líneas tienen monto negativo, el crédito legítimo del PDF está guardado en
positivo, y no existe ningún campo `signo` ni `seccion`. **El guard tal como está especificado
es imposible con estos datos**: la regla "positivo dentro de una sección de consumos no puede
ser de la familia de ingresos" no tiene contra qué evaluarse.

Quedamos, como dice la spec, con el prompt (§3) más el selector de F9.157. **No inventé un
sustituto por descripción.**

**Para desbloquearlo** hay que preservar el dato primero: que el prompt emita `montoFirmado`
(el importe con el signo del PDF) y `seccion` (el bloque de origen), persistirlos en la línea,
y recién entonces el guard es una comparación trivial. Es un cambio de extracción + esquema,
no de la capa de cuadre. No lo hice porque la spec lo pone fuera de este alcance.

---

## §3 — APLICADO Y VERIFICADO contra la API real

`CAJA SEG-PROMO` sale de la lista de reintegros; el caso especial se invierte y queda escrito
por qué; se agrega el bloque **"EL SIGNO DECIDE, NO EL NOMBRE"** con los dos renglones reales
de este PDF como ejemplo positivo y negativo.

Extracción real del PDF de `08a697e0` con el prompt nuevo (`scripts/verificarF9160.ts`, que
**extrae `buildResumenTarjetaPrompt` y `sanitizarJson` del fuente** y replica la llamada del
código real — streaming, `max_tokens: 32000`):

```
  prompt: 13163 chars | banco="BBVA Argentina" tarjeta="Visa Signature"
  líneas extraídas: 90 | totalARS=1403821.1 totalUSD=73.55

  --- §5.2/§5.3: los dos casos que la spec exige ---
  OK  CAJA SEG-PROMO (positivo, 168.542)   → tipoLinea=consumo   monto=      168542 ARS
  OK  COTO DIGITAL CRED (el de 8.870,44)   → tipoLinea=reverso   monto=     8870.44 ARS
  OK  COTO DIGITAL (el de 308.251,02)      → tipoLinea=consumo   monto=   308251.02 ARS

  --- §5.4: el cuadre con la extracción nueva (sin ningún ajuste manual) ---
  ajustesConsolidado extraídos: [{"concepto":"CR.RG 5617 30% M","montoARS":-890257.63,"montoUSD":0}]
  sumaARS=1403821.10 totalARS=1403821.1 diffARS=0.00 balanceARS=true
  sumaUSD=73.55 totalUSD=73.55 diffUSD=0.00 balanceUSD=true

  --- subtotal por sección vs el PDF ---
       (sin persona) parseado=      34698.29 | PDF=           n/d | Δ=n/d
  OK  FEDERICO      parseado=     107712.15 | PDF=     107712.15 | Δ=0.00
  OK  JUAN          parseado=     807818.44 | PDF=     807818.44 | Δ=0.00
  OK  MARIA         parseado=    1336897.92 | PDF=    1336897.92 | Δ=0.00
  OK  SOFIA         parseado=       6951.93 | PDF=       6951.93 | Δ=0.00
```

**`diffARS = 0` con `sumaARS = 1.403.821,10` y sin ningún ajuste manual**, y los cuatro
subtotales cierran exacto. Es el criterio de §5.4 completo.

**Detalle del test, no del código:** "COTO DIGITAL SUC 056 CRED" son **dos renglones distintos
del PDF con la misma descripción** — el crédito de −8.870,44 y un consumo real de +308.251,02.
La aserción los separa por monto; buscar por descripción daba un falso negativo mío, no del
prompt.

**§5.5, no regresión:** los 24 resúmenes sin líneas de la familia de ingresos ni ajustes
manuales quedan **idénticos** — el prompt solo afecta extracciones nuevas y `calcularCuadre`
no se tocó.

---

## §3.5 — AUDITADO, NO IMPLEMENTADO: `incluir:false` tiene dos significados hoy

Las cinco líneas con `incluir:false` de los 30 resúmenes:

```
5d948f9a | 2026-07 | BBVA/Mastercard Black  | impuesto |    238290.36 ARS | DB.RG 5617 30% ( 794301,20 )
b553ddf1 | 2026-07 | BBVA/Visa Signature    | reverso  |     97500.00 ARS | DREAM STUDIOS BA
f3e9e4f3 | 2026-07 | BBVA/Visa Signature    | consumo  |   1979315.00 ARS | MERPAGO*LASCANOMARIA
f3e9e4f3 | 2026-07 | BBVA/Visa Signature    | impuesto |    890257.63 ARS | DB.RG 5617 30% ( 2967525,44 )
fc31ca48 | 2026-07 | Galicia/Visa           | consumo  |        14.99 USD | AMAZON PRIME PMTS
```

**Los dos usos están mezclados**, que es la rama mala de las dos que la spec anticipa:

- **"no se debita"** — los dos `DB.RG 5617 30%`, exactamente el caso del spec (percepción que
  no corresponde porque los consumos en dólares se pagaron con dólares propios);
- **"no quiero un movimiento propio para esto"** — el reverso de DREAM STUDIOS, el consumo de
  MERPAGO y el AMAZON PRIME, que sí se debitan.

**Conclusión: hace falta un marcador distinto (`noDebitado`) y `incluir` se queda como está.**
Reutilizarlo bajaría el objetivo del cuadre por líneas que el banco sí cobra, y en dos de los
tres casos por montos grandes (1,98 M y 97,5 k), o sea que el movimiento-total de la tarjeta
saldría por menos de lo que se paga.

**No lo implementé** porque la spec lo condiciona a §1 cerrado *y verificado*, y §1 cerró
recién con el §3 verificado — pero además §2 quedó bloqueado, así que mover el objetivo del
cuadre ahora dejaría dos cambios abiertos a la vez, que es justo lo que el orden obligatorio
del §3.5 existe para evitar.

**De paso: hay 10 ajustes manuales en 7 resúmenes**, todos con el mismo concepto "Diferencia
no identificada". Tres son de importe 0 (parches vacíos); los otros van de 33.035,55 a
**2.869.572,63**.

---

## §4 — DRY RUN. Nada escrito. **4 empeoran → paré, como pide la spec.**

Separé las dos mitades de la corrección para poder atribuir cada efecto:

```
id       | período | banco/tarjeta                  |  diffARS hoy | A: reclasificado | B: A sin ajuste manual | seg | aj.man
68c7a1ea | 2026-05 | BBVA/Visa Signature            |         0.00 |    340932.00     |          0.00          |  1  |  1
5d948f9a | 2026-07 | BBVA Argentina/Mastercard Blac |         0.00 |         0.00     |     347925.73          |  0  |  1
b553ddf1 | 2026-07 | BBVA Argentina/Visa Signature  |         0.00 |    357032.00     |      89113.99          |  1  |  1
c7222865 | 2026-07 | BBVA/Mastercard Black          |         0.00 |         0.00     |      33035.55          |  0  |  1
f3e9e4f3 | 2026-07 | BBVA Argentina/Visa Signature  |         0.00 |    327914.00     |    2878033.12          |  1  |  2
fc31ca48 | 2026-07 | Galicia/Visa                   |         0.00 |         0.00     |          0.00          |  0  |  2
08a697e0 | 2026-08 | BBVA Argentina/Visa Signature  |         0.00 |    337084.00     |          0.00          |  1  |  1
f9d9b308 | 2026-08 | Galicia/Visa                   |         0.00 |         0.00     |          0.00          |  0  |  1
```

Los otros 22 (incluidos los 17 del seed) no tienen ni seguro mal clasificado ni ajuste manual:
**no cambian**.

**Lo que dice la tabla:**

- **2 pasarían a cuadrar de verdad**: `68c7a1ea` y `08a697e0` — el ajuste manual es exactamente
  2 × el seguro, así que reclasificar y quitar el parche da cero por las razones correctas.
- **2 quedan en cero y solo se les cae un parche vacío**: `fc31ca48` y `f9d9b308` (ajustes de
  importe 0).
- **4 EMPEORAN**, y por eso paré:
  - `b553ddf1` — el parche (267.918,01) no cubre todo el efecto del seguro (357.032). Queda un
    residuo de **89.113,99** que es **otro error distinto**.
  - `f3e9e4f3` — dos parches, uno de **2.869.572,63**, que compensa algo que no tiene nada que
    ver con el seguro. Residuo **2.878.033,12**.
  - `5d948f9a` (**347.925,73**) y `c7222865` (**33.035,55**) — **no tienen ninguna línea de
    seguro**. Sus parches tapan errores completamente desconocidos.

**Interpretación, que es lo importante:** los cuatro que empeoran son la prueba de que
`CAJA SEG-PROMO` **no era el único error tapado por "cerrar diferencia"**. Hay al menos tres
descuadres más, sin diagnosticar, de entre 33 k y 2,87 M.

**Y una consecuencia de método:** parchear los resúmenes viejos campo por campo es el camino
equivocado. Sus `movimientosParseados` salieron del prompt viejo, así que pueden tener otros
errores además del seguro. El camino correcto para los viejos es **re-extraer el PDF con el
prompt nuevo** (que ya sabemos que cierra al centavo) y comparar, no editar líneas sueltas.
Eso es otra tarea, con su propio dry run.

---

## Estado por sección

| § | estado |
|---|---|
| §1 | cerrado — el segundo error es el ajuste manual de 337.084 |
| §2 | **BLOQUEADO** — el signo no se conserva; guard no construido |
| §3 | aplicado y verificado contra la API real |
| §3.5 | auditado; **necesita marcador propio**; no implementado (orden obligatorio + §2 abierto) |
| §4 | dry run volcado; **4 empeoran → parada**; cero escrituras |

## Cierre

- `tsc --noEmit | grep -c "error TS"`: cliente **41** al abrir, **41** al cerrar; functions **0 → 0**.
- `npm run build` y `npm --prefix functions run build`: OK.
- **Sin deploy.** `functions/src/index.ts` cambió (el prompt), así que cuando se deploye va
  `--only functions`.
